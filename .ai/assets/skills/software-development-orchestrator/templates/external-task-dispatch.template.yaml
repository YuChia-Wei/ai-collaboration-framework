# Generated by execution-artifacts.py templates; placeholders are not execution facts.
schema_version: '1.3'
record_type: external-task-dispatch
delegation_id: <required string>
task_kind: <required string>
source:
  task_id_source: <required string>
  task_id: <required string or null>
  final_integration_owner: <required string>
objective:
  goal: <required string>
  non_goals:
  - <required string>
subject:
  repository_root: <required string>
  commit_sha: <required string>
  clean_worktree_required: <required boolean>
execution:
  working_directory: <required string>
  argv:
  - <required string>
  timeout_seconds: <required integer>
execution_packet:
  schema_ref: <required string>
  packet_ref: <required string>
  packet_sha256: <required string>
  subject_sha: <required string>
  validator_argv:
  - <required string>
  validation_outcome: <required string>
permissions:
  read_scope:
  - <required string>
  write_scope:
  - <required string>
  repair_allowed: <required boolean>
  external_mutations:
  - <required string>
  secret_values: <required string>
completion_delivery:
  primary: <required string>
  fallback: <required string>
  destination: <required string>
  progress_updates: <required string>
  max_terminal_reports: <required integer>
  report_schema: <required string>
  pre_send_validation:
    required: <required boolean>
    receipt_writer_argv:
    - <required string>
    dispatch_ref: <required string>
    candidate_ref: <required string>
    receipt_ref: <required string>
    failure_action: <required string>
    payload_binding: <required string>
stop_conditions:
- <required string>
authority_manifest:
- path: <required string>
  sha256: <required string>
