# Bounded public skill execution

Current checkpoint: [resumed report](resume-c1fb1c1f-report.md), [task](task.json), and
[resumed observations](evidence/resume-c1fb1c1f-observations.json). On source
`c1fb1c1fb07a6d246e3bcedd3cd851918f66b306`, seven unchanged families ran once
each: five passed, PR cleanup failed, and the workflow deferred-task fixture failed.
The owner authorized records-only local handoff; coordinator repair selection is next.
No full public, rc.1 pilot, native or provider acceptance follows.

Initial implementation and earlier evidence remain historical in the
[original report](report.md) and [original observations](evidence/observations.json).
The scope and source pin below describe that earlier checkpoint; this resumption
authorizes only the local fast-forward, selected execution and own workflow records.

Authority: owner dispatch, live [Issue 373](https://github.com/YuChia-Wei/ai-collaboration-framework/issues/373),
U001 and [selected P7 plan](../../design/framework-next/p7-execution-selection.md).
Owning route: ai-context-governance. Declared dispatch: GPT-6 Astra / ultra;
this is dispatch provenance, not independent runtime attestation. One conversation,
one writer, no sub-agents or callbacks. Starting commit:
`f8f0d073e6442df8a280c5941a2c7a1d6ed7060b`.

Own the three public test modules, public dispatch arm, test README and this
workflow only. Use unchanged helper and exact committed resource bytes. No
assembly, installation, native acceptance or source repair is inferred. Source
remains pinned even though coordinator later reported PR 374 merged separately.

Implement selected C4/C6 and T1-T7; execute one family at a time in unique helper
children under `F:/framework-next/p7-runs/373-public`. Preserve actual failures,
unexecuted phases and exact process streams. No unchanged failure retries or
live provider invocation. The T5 design's conceptual active state is reconciled
to actual `draft -> planned -> in_progress -> completed`, explicitly accepted
by coordinator in this task. Source transition requirements remain unchanged.

Acceptance: all selected assertions actually complete with no skips, failure,
missing work or cleanup failure; otherwise nonzero and truthful partial results.
Each family has one sequential test so a common setup failure stops its dependent
cases. Configuration uses one small project; provider simulation replaces only
the adapter transport and is labelled synthetic. Existing FixtureRun bounds
remain authoritative. Direct syntax, references, Git scope/diff and complete
planned commit-message validation precede local commit handoff.

U001 adapts the owning templates proportionally. Legacy/full/history/native,
audit/lease packets, hosted checks, CI restoration, release and adoption remain
`deferred-by-owner`, owner program 322 coordinator / P7; next action is separate
selection/adoption after inspecting actual results. No push/PR/tracker mutation.
