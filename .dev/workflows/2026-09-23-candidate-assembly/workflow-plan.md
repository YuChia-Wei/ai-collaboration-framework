# Candidate assembly implementation

- Workflow: `2026-09-23-candidate-assembly`; Issue [#331](https://github.com/YuChia-Wei/ai-collaboration-framework/issues/331).
- Owner: `ai-context-governance`, explicitly assigned by U001/P2-B.
- Branch: `codex/2026-09-23-candidate-assembly`; base `main`; worktree `F:/framework-next/331`.
- Starting commit: `a1d8b750b8c81fd17697a39e04b55c35c4f3c4dc`; persistent Git database read back under `C:/Github/YuChia/ai-collaboration-prompts-dotnet-backend/.git`.
- Created: `2026-09-23T01:44:29+08:00`; updated: `2026-09-23T01:59:15+08:00`.
- Status: completed for bounded local code delivery; product verification is `deferred-by-owner`.
- Template: `.ai/assets/skills/ai-context-governance/templates/ai-context-maintenance-workflow-plan-template.md`, version `1.2.0`, adapted for U001.
- Execution: OpenAI Codex, `gpt-6-astra` / `ultra` as explicitly dispatched; no independent runtime attestation claimed. No sub-agents.

## Authority and scope

Read [U001](../../assessments/ASM-20260923-00-6oq/execution-plan.md), [temporary override](../../standards/FRAMEWORK-REDESIGN-EXECUTION-OVERRIDE.md), [handoff](../2026-09-23-framework-redesign-control/handoffs/issue-331.yaml), [P1/P2 scope](../2026-09-23-framework-redesign-control/reports/p1-integration-and-p2-scope.md), P1-A portable contracts and P1-B source-layout design, and the live Issue before implementation. The handoff's `b4d54966` is the integrated P1 reference; the dispatched starting HEAD is `a1d8b750`.

Own only `src/distribution/`, `src/profiles/`, `src/adapters/codex/`, `tools/build-development.py`, `.dev/design/framework-next/distribution-implementation/` and this workflow. Deliver exact immutable Git selection, closed resource inventory, Codex projection, development assembly and maintainer invocation. No installed changes, apply/delete/migration/recovery engine, root changes, shared indexes, release, push or provider mutation.

This one-task workflow preserves an explicit cross-conversation handoff, the #330 member integration dependency and P7 verification deferral. It does not create audit tasks to satisfy suspended gates.

## Completion and checks

The implementation must reject invalid/escaping/colliding paths, non-regular Git members, absent or inconsistent resources, unselected required dependencies and unsupported metadata. It must preserve Git bytes/modes, create unique contained outputs, and produce truthful development identities and installed-only runtime references when eventually invoked. Final exact #330 members are reconciled by the coordinator before first push.

Allowed checks: strict UTF-8, JSON/YAML read-back, `ast.parse` without imports/execution, reference/content inspection, Git diff/status and the complete planned commit-message format exception. All product invocations (including help), builds, tests, audits, old validators and CI are `deferred-by-owner` under U001 to program #322 coordinator / P7. Required validation behavior is implemented in the product; it is not executed in this task.

## Resume

Task `ASSEMBLE-001` is complete for the authorized code/document scope. See [delivery report](reports/remediation-report.md) and [handoff](handoff.yaml). The eight coordinator-selected Lesson members are declared exactly, including `references/example.md`; no helper/interface extension is pending. #330 source is intentionally absent in this branch, so coordinator integration must read back its final metadata against this manifest before first push. No build or installation result is claimed. Coordinator owns integration and P7 owns behavioral verification. Root runtime remains active until later cutover.

The delivery commit is the commit containing these final artifacts; the executor reads back its exact HEAD and message after committing and includes them in the callback. Shared workflow/handoff indexes remain coordinator-owned. The proposed shared index row is in the delivery report, not written into a shared index.
