# Optional context maintenance design delivery

- report_id: design-delivery-2026-09-23-optional-context-maintenance-design
- workflow_id: 2026-09-23-optional-context-maintenance-design
- owner_skill: ai-context-governance
- status: final
- template_source: .ai/assets/skills/ai-context-governance/templates/ai-context-remediation-report-template.md
- template_version: 2.0.1
- adaptation: U001 design-only report; no fabricated baseline/post-audit or finding closure.

## Result

[Design](../../../design/framework-next/optional-context-maintenance/README.md)
recommends six decisions D352-01..06: independent optional instruction packages,
prose/export default plus selected project-format reuse, protected managed content,
minimal state with the existing rule/decision owner, retained source/legacy recovery,
and optional public P3 semantic candidates. No new assessment schema is selected.

The design compares all three persistence options, supplies audit/compare and
propose/apply entry/method drafts, defines exact future file owners and proposes
P7 observations. #316 source authoring remains separate and OPEN at observed time.
No acceptance criteria for #316 or program findings are declared resolved here.

## Evidence and preparation outcomes

- Initial explicit F: identity read matched root/branch/full starting SHA and
  common Git dir C:/Github/YuChia/ai-collaboration-prompts-dotnet-backend/.git;
  porcelain was empty.
- Initial P5 read used an incorrect reports path and failed. The scope document
  and coordinator correction identify the tracked design path, subsequently read.
- Initial sandboxed GitHub read failed at the restricted loopback proxy. A normal
  scoped escalation allowed read-only #352/#316 read-back; no credentials changed.
- A guessed auditor semantic-customization reference path did not exist. The actual
  skill declares governance's semantic-customization-lifecycle.md, which was read.
  No absence conclusion relies on the failed lookup.
- A first multi-document shell write failed before process creation with Windows
  error 206 (command length). An apply_patch attempt could not create the F:
  parent directory. Smaller scoped escalated writes succeeded in the same F:
  roots. No alternate worktree/drive or shared-file edit was used.
- Discovery used explicit tracked files and direct reads. Shared graph SHA/coverage
  was not established and no graph was queried/reindexed; the assignment prohibits
  shared index mutation. This is an explicit tracked-file fallback, not a graph
  completeness claim.
- Current policy/tool/P3 evidence is pinned in source-evidence.json. Static source
  inspection did not import or invoke product tooling, including help.

## Actual checks

- Direct helper: python -B .dev/workflows/2026-09-23-optional-context-maintenance-design/direct-checks.tmp.
  Passed for 11 strict UTF-8 files, two JSON files, one YAML locator, two YAML
  frontmatter blocks, 22 relative Markdown links and 17 pinned source identities.
  JSON duplicate/nonfinite values are rejected; links are resolved relative to
  their actual file and stay within the repository. Source Git blobs and raw
  checkout hashes are compared with the pinned evidence. No product imports.
- git diff --cached --check passed. Staged name review contains 11 additions only
  within the authorized two roots. Direct design/report diff inspected.
- Full planned message: python -B .ai/scripts/validate-git-commits.py --message-file
  F:/framework-next/352/.dev/workflows/2026-09-23-optional-context-maintenance-design/commit-message.tmp
  --workflow-id 2026-09-23-optional-context-maintenance-design.
  Output: Git commit validation passed for planned message.
  Exact UTF-8 message SHA-256:
  3ae131349078c89218c622666bed8123604684de3cad2777e663ea8790998700.
- Both helper and message are ignored under the existing *.tmp rule, are outside
  the staged payload, and were not put in shared local storage. Git hooks have
  only inactive sample files; no product gate is invoked through the commit.
- Provider observations preserve actual #352 and #316 OPEN states at their saved
  times. No inference about later Issue/Project state or acceptance is made.

The workflow/task are completed only for this bounded design under U001. Their
final bookkeeping changes receive direct syntax/reference/diff rechecks before
commit. Commit transport and fresh clean/HEAD read-back are returned in final;
no self-referential future commit identity is embedded in the tracked documents.

## Deferred verification

All product execution (including help), schema validation, tests/fixtures,
build/package/install/migration, legacy validators, independent audit/review/lease
machinery and CI are deferred-by-owner under U001, owner program #322 coordinator
/ P7. Next action: select the implemented contract's narrow checks/actual-use
observations, run only under that phase's authority and retain real outcomes.
Syntax/content checks are not behavior, independent review or hosted acceptance.

## Handoff and remaining decisions

Coordinator owns recommendation selection, future source assignment, shared
index/mapping, #316 reconciliation, P6 installation/cutover and push/PR/online merge.
Proposed shared index row and exact slices are in implementation-plan.md.
No new schema, source code or provider change is part of this delivery.
Local commit SHA, clean status and branch/worktree will be read back in final.

Updated at: 2026-09-23T10:18:43.0502001+08:00

Final bookkeeping recheck first failed in the direct helper because its initial
no-tracked-diff assertion also rejected the now-staged authorized additions.
This was a check-preparation defect, not a product failure. The helper was narrowed
to require every changed/untracked path within the two authorized roots while
retaining exact HEAD/branch and all 17 source blob/raw-byte checks. The failed
assertion is retained here; the corrected check is rerun after that material fix.

Corrected recheck passed with the same 11 files, 22 links and 17 source identities;
staged/unstaged whitespace checks also passed. No product validation was run.
