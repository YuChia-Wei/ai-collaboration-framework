# Issue 371 local source repair handoff

- Report/workflow: `2026-09-23-engine-source-pin`; created `2026-09-23T15:42:21+08:00`; updated `2026-09-23T15:45:18+08:00`.
- Owning route: `ai-context-governance`; report template `.ai/assets/skills/ai-context-governance/templates/ai-context-remediation-report-template.md`, version `2.0.1`, proportionately adapted under U001.
- Authority: live [Issue #371](https://github.com/YuChia-Wei/ai-collaboration-framework/issues/371) read OPEN; [coordinator task](../2026-09-23-framework-redesign-control/tasks/ISSUE-371.json), [U001 override](../../standards/FRAMEWORK-REDESIGN-EXECUTION-OVERRIDE.md).
- Worktree `F:/framework-next/371`; branch `codex/2026-09-23-engine-source-pin`; clean starting HEAD `fe6c63f87cb54e166fc796fd315f11e3c2212823`. Common Git directory is the existing persistent `C:/Github/YuChia/ai-collaboration-prompts-dotnet-backend/.git`.
- Coordinator-selected OpenAI Codex / gpt-6-astra / ultra conversation; declared provenance, not separate runtime attestation. One writer; no agents, new tasks or callbacks.
- Local implementation is complete. CR-001 disposition is **partially-resolved pending independent affected-source review and public/native evidence**. No Issue closure or integration is implied.

## Change and source identity

The [entry](../../../src/tools/maintain_framework.py) retains each local module's bytes immediately after its existing path/stat/size/hash admission. `_VerifiedSourceFinder` intercepts the `distribution` package and its allowed submodules. It compiles those retained bytes directly with the verified source filename, preserving module origins and package search metadata. The finder remains installed through dispatch for lazy imports, refuses unknown local names before normal path finders, and leaves host imports on their existing path. Handled bootstrap refusal and dispatch completion/failure remove the finder. No cache is read, rewritten or deleted by the new product loader.

The closed ten-file EnginePin is unchanged. Only the entry's loader code changes; nine distribution source files remain raw-byte-identical to reviewed product `38e6458f8d3e81dc2568daf1fa467571fb529fee`. `_direct`, `_pairs` and `_reject_number` are AST-identical; root/type/size/hash/stat, preloaded-module and PyYAML checks remain in place. Installation, recovery, origin verification in installation_state, package metadata, allowed members, helper and runner are untouched. Test cleanup removes only its successfully owned disposable fixture directory, not ambient engine caches.

The [fixed CR-001 report](../2026-09-23-installation-source-review/report.md) remains at `6de20bfeb059d74f1ba0ede96c28d312db04235f`. [Evidence](evidence.json) records all ten reviewed blobs and current raw SHA-256 values. Entry SHA-256 changed from `f5725eb689979df7a58a3bb35acb440c6485aa84075a92b4d0101cb6891e282e` to `8030be53ee2988e21628ad11f348ad18a5f4114a0fcdb79e96dc473e23bdcf3d`. New test SHA-256 is `bfe916dc9f86c08b50549ed1b750b8a798c68c62f62f4ca96efd90ecad0ae249`. The delivered helper matches `070a47335ffce99d31bd83e487447942539e4a9f` exactly.

Graph-first discovery indexed `framework-371` with persistence disabled at the unchanged starting HEAD. The graph exposed no commit attestation and excluded `src/tools`; exact tracked-file reads covered that entry and the selected test/helper. Graph discovery found the state owner and 13 distribution File nodes. Raw Git/blob comparison supplies identity evidence; graph search absence was not treated as proof.

## Selected checks and limits

Actual host: Python 3.13.14, PyYAML 6.0.3, isolated mode and bytecode writes disabled. No dependency installs or TEMP/TMP changes.

1. `python -I -B F:/framework-next/371/tests/framework_next/test_engine_source.py --output-root F:/framework-next/p7-runs/371-engine-source`: **9 tests passed**, no skips/errors/failures, 1.279 seconds. The unchanged #368 helper recorded 96 files / 79,978 observed logical bytes, eight Git subprocesses, 1.285-second fixture lifetime and successful owned-run cleanup.
2. `python -I -B F:/framework-next/371/tests/framework_next/test_engine_source.py --output-root F:/framework-next/p7-runs/371-engine-source --public-entry`: **one attempt, blocked before dispatch**, child exit 1, `source-bootstrap`, `outcome=unsupported`, `changed=false`, empty stderr. The tool reported shell exit 1; the harness source returns 2 for this retained non-acceptance outcome. These are recorded separately. No retry.
3. Direct `ast.parse` for both Python files, UTF-8/JSON/YAML parsing for the local records, local-reference existence, raw source identity, allowlist/boundary AST comparison, selected path/diff inspection and `git diff --check` are the narrow checks. The complete planned-message command `python -I -B F:/framework-next/371/.ai/scripts/validate-git-commits.py --message-file F:/framework-next/371/.dev/workflows/2026-09-23-engine-source-pin/commit-message.tmp --workflow-id 2026-09-23-engine-source-pin` passed (exit 0); exact message SHA-256 is in evidence.json. No legacy range/matrix gate is claimed.

The [new tests](../../../tests/framework_next/test_engine_source.py) call the real bootstrap/loader. Synthetic bootstrap cases replace `_direct` explicitly so the loader can be exercised on this F: host; they do not establish native path admission. They prove source execution across package and transitive imports despite nine valid unchecked-hash caches, a valid timestamp cache, and a post-verification source-file change. Controls use Python's real SourceFileLoader to prove the different caches are acceptable. Tests also exercise unknown/lazy module refusal, existing hash/type/closure/preloaded refusals, a synthetic path failure, origin/search metadata and host-module shadow prevention. A separate direct loader case imports the real unchanged nine-module distribution closure without calling installation operations.

The public attempt used a tiny separate Git checkout containing the exact current ten engine source files. Its source pin stayed unchanged while a deliberately different valid installation_io cache raised a sentinel under the control loader. The actual entry was invoked unpatched with no project or installed roots and a bounded inspect request. It refused in bootstrap on F:, before project dispatch. Source bytes, pin and cache were unchanged afterward. This generic diagnostic does not isolate the precise failing bootstrap predicate; it is compatible with the separately recorded F: path-resolution limitation, not proof of that root cause. No public execution of pinned source or native installation acceptance is established.

Retained public residue: `F:/framework-next/p7-runs/371-engine-source/fn-53f9ba958c7e4e608532c807c08cd7a2` (36 files / 216,288 logical bytes; five Git and one Python subprocess; 0.378 seconds). Request/stdout/stderr are inside its `public-entry` directory; the portable response, identities and metrics are also copied into evidence.json. Preserve this residue for owner inspection; no deletion or repeat trial was performed.

Initial sandboxed GitHub read failed against restricted proxy `127.0.0.1:9`; normal read-only escalation succeeded. Initial narrow graph queries returned no matches; corrected name/File queries supplied the selected discovery. Neither event is a product pass. The first inline AST/constants comparison failed with `AttributeError: 'Attribute' object has no attribute 'id'` because its helper assumed every assignment target was a Name. It stopped before message validation. Comparing whole assignment ASTs fixed that check; the second attempt passed without changing product/test source. The precommit raw/staged-byte check then stopped before commit because Git normalized the newly authored CRLF workflow records to LF. Only those records and the ignored message were normalized to LF; the exact message and staged bytes are checked again before committing. Product/test source did not change. There were no focused test failures to erase and no automatic approval rejection in this task.

## Local checkpoint and next owner

Changed paths are exactly the entry, new test, and this workflow's `workflow.yaml`, `task.json`, `report.md`, `evidence.json`. Resolve the local handoff with `git -C F:/framework-next/371 log -1 --format=%H -- .dev/workflows/2026-09-23-engine-source-pin/report.md`, then verify branch/status and the evidence hashes. The containing commit is the checkpoint identity, avoiding a self-referential SHA in this record.

Coordinator sends the changed entry plus tests/evidence to the original #370 task for independent affected-source review. That review must account for the changed loader against the earlier CR-001 subject; this self-check is not independent review. Public/native evidence remains pending with the coordinator/P7. The stopped #368 GitSource/assembly/metadata and #369 governance batches remain stopped in their original tasks awaiting direct owner approval; this repair does not take over their files or repair path resolution.

CI, legacy validators/matrices, formal audit/receipt/lease/native handoff tooling remain **deferred-by-owner**, authority U001, responsible owner program #322 coordinator / P7; next action is the owner-selected verification/restoration decision. No push, PR, Issue/Project mutation, CI mutation, release/publication, root adoption or downstream adoption was performed. Stop after this coherent local handoff.
