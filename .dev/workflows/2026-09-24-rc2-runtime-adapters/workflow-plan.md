# S4 runtime adapter local delivery

- Workflow: `2026-09-24-rc2-runtime-adapters`; Issue [#404](https://github.com/YuChia-Wei/ai-collaboration-framework/issues/404); program [#322](https://github.com/YuChia-Wei/ai-collaboration-framework/issues/322).
- Input: integrated S1 #401/#402 contract `173b2523767aece5004bc57c6e0cbba12898039a`, especially `.dev/design/framework-next/rc2-contracts/implementation-handoff.md`.
- Branch: `codex/2026-09-24-rc2-runtime-adapters`; base `9247f444eb14a86849c66078cd7fa366dc098379`. This record binds to its containing local commit, without a self-referential SHA.
- Execution: one coordinator-dispatched short sub-agent, `gpt-6-sol` / `high`, owning only the declared S4 paths. Detailed dispatch identifiers and local paths remain in ignored local notes.
- Lifecycle: bounded S4 implementation completed; first push, PR, online merge, Issue/Project changes, S3 integration, and S6/P7 verification remain coordinator-owned.

## Accepted S4 units

| ID | Result | Evidence and limit |
| --- | --- | --- |
| S4-A1 | Legacy Codex `project_entry` body and original template bytes retained. | Original template SHA-256 and renderer source identity are in [S3 handoff](s3-handoff.json). The legacy function is not routed through v2. |
| S4-A2 | Separate Codex and Claude `project_entry_v2` functions and templates use fixed `aicf-` roots and the seven existing keys. | Direct AST parsing and template placeholder inspection only; no renderer invocation. |
| S4-A3 | Templates link selected installed skill members, escape description as a scalar, and explain metadata-4 optional knowledge availability. | Source inspection only; installation and runtime behavior deferred. |
| S4-A4 | Exact template digest and selected-member path checks reject a mismatched renderer/template or unrelated member. Immediate unit cases were authored. | The cases in `tests/framework_next/test_rc2_adapters.py` are unexecuted under U001. |
| S4-A5 | Exact adapter inputs, hashes, output rules, legacy identity, and inventory expectations returned to S3. | [S3 handoff](s3-handoff.json); S3 owns shared manifest, callers, format/API dispatch, generator and engine closure. |
| S4-A6 | Codex and Claude actual discovery/routing observations are distinct S6 cases. | Generated bytes establish neither runtime; actual observations remain `deferred-by-owner`. |

## Actual local checks

Python `ast.parse` directly parsed both renderer files and the new test file. Both new templates decode as strict UTF-8 and expose exactly the seven supported `string.Template` identifiers. Git showed only assigned source/test/workflow paths; `git diff --check` reported no whitespace errors. The complete planned commit message is validated separately before the local commit. These checks are syntax and source-shape evidence only.

## Deferred and handoff

Behavioral tests, legacy verification, package/installation trials, independent review, hosted contexts, CI, actual Codex discovery and actual Claude discovery: `deferred-by-owner` under U001, responsible owner program #322 coordinator / P7 with selected S6 owners. Next action: S3 integrates exact v2 adapter descriptors and explicit format dispatch, then S6/P7 select and execute appropriate focused cases. No old runtime directory is removed by S4.

Coordinator integration row proposal: `2026-09-24-rc2-runtime-adapters | #404/#322 | local-change-implementer | bounded S4 source delivered locally; U001 behavior deferred | review commit and consume s3-handoff.json, then integrate S3 and select S6/P7`.
