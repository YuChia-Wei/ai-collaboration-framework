# S1 local delivery

Status: bounded S1 document delivery completed under U001; direct checks and exact
planned-message validation passed. Git delivery is bound to the containing commit.
The delivery is scoped to Issue #401 / program #322 under U001. Product behavior,
runtime discovery, adoption, publication and provider integration are not performed.

Owned changed roots:

- `.dev/design/framework-next/rc2-contracts/`
- `.dev/workflows/2026-09-24-rc2-selection-contract/`

The exact local commit is supplied in the local coordinator handoff; this tracked
record is bound to its containing commit to avoid a self-referential SHA. Branch:
`codex/2026-09-24-rc2-selection-contract`; base:
`6f5a13046f1978ba4b8701e4ae772faecde2c2ff`.

See [workflow-plan.md](workflow-plan.md) for A1–A6, preserved failures and limits;
[checks.json](checks.json) records direct actual outcomes. Legacy/full/formal/
behavioral/native/hosted/CI verification is `deferred-by-owner`, responsible owner
program #322 coordinator / P7. No pass is inferred from readability or Git state.

Coordinator integration row proposal (do not interpret as a shared-index edit):

| Workflow | Issue | Owner | Bounded state | Next action |
| --- | --- | --- | --- | --- |
| 2026-09-24-rc2-selection-contract | #401 / #322 | ai-context-governance | S1 document contract delivered locally; U001 verification deferred | Coordinator review/integrate, assign S2–S5 from exact handoff, select S6/P7 separately |

No remaining owner-sensitive S1 decision. Preserve the catalog's 11 unregistered
hash discrepancies as S2 input, all retained source/target duties, and the explicit
reference/member maps. S2/S3 may repair relocation metadata and links within the
selected meaning; substantive contract/meaning changes return to the coordinator.
First push, PR, merge and Issue/Project state changes remain coordinator-owned.

Actual direct checks: 40 UTF-8 files, 30 JSON, 2 YAML, 36 local Markdown links,
52 schema-document references, complete 194-file profile coverage, 235 collision-free
planned package members, owned-only staged scope and clean diff whitespace.
This is document evidence; no JSON Schema compliance, product behavior or runtime
discovery was executed. The final local handoff supplies HEAD and clean status.

## S1-R1 follow-up

Disposition: corrected in the contracts; implementation/behavior remains deferred.
Original delivery commit `e27ff21cf06c91abdf5fde4f70c1bddb632b1898` and its
[original checks](checks.json) are retained unchanged. The original 40-file check
counts above belong to that commit; they are not a rerun claim for this correction.

The correction separates preserved legacy Codex project_entry/skill-entry.md.template
from new Codex and Claude project_entry_v2/skill-entry-v2.md.template. S4 owns
renderers/templates; S3 owns exact format/API dispatch, callers, manifests and engine
closure. New legacy mode returns unsupported-write for rc.2-only shapes or requested
metadata-4 output; --profile keeps its old meaning. Original pinned implementations
retain their behavior. Schemas/examples/inventories are unchanged.

See [the correction checks](s1-r1-checks.json) for narrow actual evidence. The next
local handoff supplies the new full HEAD and clean status. No new owner decision;
first push and provider integration remain coordinator-owned.

The same correction aligns lifecycle_contract 1.0: workflow status and current_phase
are both completed for this bounded document delivery. Existing integration,
completion_scope and task next_action retain the pending first-push/provider work.
This alignment uses direct artifact reading, not formal lifecycle validation.
