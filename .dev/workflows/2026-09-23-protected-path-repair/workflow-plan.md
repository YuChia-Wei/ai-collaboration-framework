# Protected path repair

Issue [#383](https://github.com/YuChia-Wei/ai-collaboration-framework/issues/383), program #322.
Owner route: local-change-implementer; applicability: framework-source.
Baseline: `cc3189193788750277269f63f3753a639becc3c6`.
Declared execution: OpenAI Codex, gpt-6-astra / ultra, one independent task;
this dispatch provenance is not independent runtime attestation. No subagents.

## Scope and evidence

Repair `_protected_path` and reuse the existing private Windows long-name query
in the already pinned state module. Preserve exact spelling, containment,
plain stable ancestry, hardlink rejection and raw byte/absence admission.
The original evidence is N382-F01 at
`878cefc7471ae77d80ddc3f1d358c2bce2704b95`,
`.dev/workflows/2026-09-23-native-maintenance-checks/report.md` and
`evidence/protected-path-diagnosis.json`. Those original failed roots stay intact.
One focused regression file and these issue-owned records are the allowed radius.

## Selected sequence

1. Implement the guarded fallback; simulate ordinary resolution, WinError 1,
   spelling/containment/link/reparse/identity/other-error refusals and protected bytes.
2. Check syntax, readability, scope and diff; validate the exact commit message;
   commit the repair before actual execution.
3. Once, build a real Lesson candidate and run public plan on F: with all five
   existing protected inputs and a fresh full ten-file raw engine pin.
   Preflight one exclusive child under `F:/framework-next/p7-runs/protected-path-383`;
   cap 96 observed files / 4 MiB / eight public/helper launches and retain evidence.
4. Record actual results and hand off local commits before first push.

No apply, recover or interruption is selected. #382 owns the later native rerun.
Unselected legacy/full/history/formal/audit/lease/effective-rule-packet/hosted
checks remain **deferred-by-owner**, U001, owner program #322 coordinator / P7;
next action is separately selected integration and P7 restoration/adoption.
Local completion does not close the online Issue, establish whole-P7 acceptance,
restore CI, install a target, or authorize publication.

## Current state

Bounded implementation and the selected execution are complete. Repair checkpoint:
`3c0832b89d73cfedfc625462d167a3811fdf7931`. Nine focused simulated regressions
passed. One actual public plan passed from that clean commit; see
[report.md](report.md) and [raw evidence manifest](evidence/manifest.json).
The record commit leaves product/test bytes unchanged and does not relabel
that executed source as a later commit. First push remains coordinator-owned.
The first sandboxed GitHub read failed at the configured proxy; the subsequent
scoped read-only query succeeded and observed Issue #383 OPEN with matching scope.
