# Repaired-source native continuation

Coordinator explicitly selected fast-forward from clean `878cefc7471ae77d80ddc3f1d358c2bce2704b95` to `cb41982dde3af9d746624b6939f26fd2883623af`, original branch, new exclusive child and the six native cases once. U001 ownership/no-agent/no-provider boundaries remain. The original 63bb8cce result and its final report remain historical evidence.

## Actual full-selection attempt on cb41982d

Exact unchanged command: `python -I -B tests/framework_next/run.py --layer native-windows --native-root F:/framework-next/p7-runs/native-w01/382`.

New child: `cb3d1c6c` under the same native/recovery/observation parents. Public inspect/plan/hash+membership refusal passed; fresh apply installed all ten exact members with lock/protected/sentinel/object integrity; same-content no-op preserved byte/mtime/mode/control/operation state; real public drift and same-byte collision refusals passed.

The writer helper printed held=true. Before dispatching the competing public apply, the driver called its ordinary file inventory, which tried reading the empty guard covered by the helper's native exclusive range. Windows returned PermissionError. The driver terminated only its owned helper; no product failure is inferred from this test-reader error. The first four cases remain passed, writer-exclusion failed, and interruption is unexecuted (zero attempts). Full selection remained exit 1.

[Result](evidence/resume-cb41982d/result.json), [raw calls](evidence/resume-cb41982d/calls.jsonl), [full raw pin](evidence/resume-cb41982d/engine-pin.json), [hash manifest](evidence/resume-cb41982d/manifest.json). The retained operation is `i-91297471d8510b6178a32e6297e02d04`, with 14 content objects; no marker remains. Counts: 11 public/helper launches, five driver Git calls, peak one live owned child, 99 observed file names / 1,010,204 logical bytes at runner close, one operation root. These are snapshots, not physical I/O or performance evidence.

## Bounded driver repair and authorized continuation

Only the reserved regular single-link zero-length guard uses its observed zero length to derive the empty digest, avoiding reads of its locked range. Identity/size/mode/mtime checks remain. Other file hashing is unchanged.

The exact private continuation verifies prior source/case boundary, real helper-held output, no prior competing public apply or interruption, and equality of all ten engine file raw hashes. It retains the actual cb41982d development candidate and installed fixture; it does not rebuild or reapply successful setup. A fresh engine pin names the new clean driver commit. Candidate source and engine source are reported separately (the accepted API permits distinct immutable sources). A fresh no-op plan admits the real exclusion observation on the unchanged prior fixture. Interruption uses a new exclusive project/recovery child and that current pin.

```text
python -I -B tests/framework_next/test_native_windows.py --resume-tail F:/framework-next/p7-runs/native-w01/382 C:/Github/YuChia/ai-collaboration-prompts-dotnet-backend/.dev/ai-context/local/p7/n382/observations/cb3d1c6c
```

This one-task continuation is hard-bound to the observed driver failure. Its result selection contains only writer-exclusion and interruption-recovery; it cannot print a fabricated six-case single-run pass. A missed interruption remains non-pass; no retry-until-green. Fresh apply and later incomplete-operation recovery remain separate from the completed earlier operation. Original outputs and transcripts are preserved. This is the expressly permitted driver-fix continuation after material change, not a third unchanged full-suite retry.

## Actual tail completion

The driver repair was committed as `ef4ffd9f286f49b6055d9a321abbd134b263ced8`. Direct AST/record parsing and an explicit snapshot check confirmed every other file is read while the empty reserved guard is not. The exact full planned message was validated before commit. The tail command above then ran once on that clean commit, allocated child `a0292789`, and returned **exit 0 for its two selected cases**.

- Native exclusion: the private helper held the original guard; the real public apply returned `blocked / writer-lock-unavailable` while that helper remained alive. Terminating only the helper released the native handle. A new actual public apply returned `unchanged`. The original guard identity/mtime and all project byte/mtime/mode snapshots were unchanged.
- Native interruption: one unmodified public apply was launched against a new tiny project. Native directory notification observed the actual `framework.operation`; the driver read it and terminated that owned process once. The retained observation has the genuine marker, guard and five protected/sentinel fixture files, no lock and no managed member yet. This proves interruption after marker publication, not a per-member or mid-replacement sweep.
- Recovery: public inspect reported `recovery-needed`; the exact same full engine pin, observed operation hash, marker hash and absent-lock binding admitted public `recover` direction `finish`. It reported ten added members; all ten candidate bytes, complete lock, protected inputs, unknown sentinel, marker removal and 14 durable objects were verified. Project readiness remains `not-assessed`.

The original `cb3d1c6c` candidate source remains `cb41982dde3af9d746624b6939f26fd2883623af`. Tail engine source is `ef4ffd9f286f49b6055d9a321abbd134b263ced8`; all ten engine file raw hashes are exactly equal across those commits, verified before dispatch and after execution. The full EnginePin source field is refreshed, and the new interrupted operation/recovery both bind the tail pin. No old completed operation was used to roll back or recover another state.

[Two-case result](evidence/tail-ef4ffd9f/result.json), [raw calls](evidence/tail-ef4ffd9f/calls.jsonl), [full tail pin](evidence/tail-ef4ffd9f/engine-pin.json), [actual interrupted state](evidence/tail-ef4ffd9f/interrupted-state.json), [final inventory](evidence/tail-ef4ffd9f/final-readback.json), [hash manifest](evidence/tail-ef4ffd9f/manifest.json). Raw tail pin-file SHA-256: `c2b8113b0eab3ce3da93904b65f5d660e3732adb807f1cc64da89a5de7f3c9de`.

## Selected evidence reconciliation

| Case | Exact passing source | Evidence |
| --- | --- | --- |
| entry-pin | cb41982dde3af9d746624b6939f26fd2883623af | Actual inspect/plan and two bootstrap refusals |
| fresh-apply | cb41982dde3af9d746624b6939f26fd2883623af | Ten installed members, lock/marker/protected/unknown/object read-back |
| same-content | cb41982dde3af9d746624b6939f26fd2883623af | Unchanged, no managed/control/operation churn |
| drift-collision | cb41982dde3af9d746624b6939f26fd2883623af | Actual public owned-drift and unowned-collision refusal on labelled authored fixture states |
| writer-exclusion | ef4ffd9f286f49b6055d9a321abbd134b263ced8 | Native lock refusal and admission after owned helper termination |
| interruption-recovery | ef4ffd9f286f49b6055d9a321abbd134b263ced8 | One genuine marker interruption and exact same-engine finish |

All selected cases now have actual evidence across the authorized continuation. The original full attempt still reports exit 1, and the tail reports exit 0 only for two cases. This is **not one six-case passing run**. [Machine-readable reconciliation](evidence/native-completion-summary.json) keeps the two subject identities and prior failures explicit.

## Bounded accounting and retained residue

The tail launched eight public/helper children, seven driver Git commands, peak two simultaneous children, one operation and one interruption. Across all three retained runs, including the original product failure: **22 public/helper launches, 17 driver Git commands, peak two children, two operations, 187 retained files / 1,958,102 logical bytes** including the final inventory. One removed actual marker gives 188 observed distinct file names. These remain within original caps. Opaque nested launches and otherwise unobserved transient creations remain unavailable, not zero. No performance claim.

Retain `63bb8cce`, `cb3d1c6c`, and `a0292789` under all three original parents. Tail recovery operation is `i-d5a469490bcc125ce1993e050494fce3`, operation SHA-256 `3a26da3ff7a1a67c1393aefe3e852f8ba253ede54c89513aae4a5f70333e3592`; the earlier completed operation `i-91297471d8510b6178a32e6297e02d04` remains. Both complete installed fixtures are marker-free. No driver-owned child remains running. No cleanup or prior-evidence mutation was performed. The final record commit changes records only and does not relabel its HEAD as the executed engine.

## Remaining owner decisions

Coordinator may integrate/review these local commits and proceed under the separately selected complete target pilot governance. The development Lesson fixture does not establish full/versioned candidate adoption, rc.1-to-stable upgrade, target readiness, broader platforms or power/storage-loss safety. No remaining selected native timing gap is claimed. CI/provider/release/publication and all unselected U001/P7 gates remain `deferred-by-owner`. No push, provider write or target activation occurred.
