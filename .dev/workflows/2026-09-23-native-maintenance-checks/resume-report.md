# Repaired-source native continuation

Coordinator explicitly selected fast-forward from clean `878cefc7471ae77d80ddc3f1d358c2bce2704b95` to `cb41982dde3af9d746624b6939f26fd2883623af`, original branch, new exclusive child and the six native cases once. U001 ownership/no-agent/no-provider boundaries remain. The original 63bb8cce result and its final report remain historical evidence.

## Actual full-selection attempt on cb41982d

Exact unchanged command: `python -I -B tests/framework_next/run.py --layer native-windows --native-root F:/framework-next/p7-runs/native-w01/382`.

New child: `cb3d1c6c` under the same native/recovery/observation parents. Public inspect/plan/hash+membership refusal passed; fresh apply installed all ten exact members with lock/protected/sentinel/object integrity; same-content no-op preserved byte/mtime/mode/control/operation state; real public drift and same-byte collision refusals passed.

The writer helper printed held=true. Before dispatching the competing public apply, the driver called its ordinary file inventory, which tried reading the empty guard covered by the helper's native exclusive range. Windows returned PermissionError. The driver terminated only its owned helper; no product failure is inferred from this test-reader error. The first four cases remain passed, writer-exclusion failed, and interruption is unexecuted (zero attempts). Full selection remained exit 1.

[Result](evidence/resume-cb41982d/result.json), [raw calls](evidence/resume-cb41982d/calls.jsonl), [full raw pin](evidence/resume-cb41982d/engine-pin.json), [hash manifest](evidence/resume-cb41982d/manifest.json). The retained operation is `i-91297471d8510b6178a32e6297e02d04`, with 14 content objects; no marker remains. Counts: 11 public/helper launches, five driver Git calls, peak one live owned child, 99 observed file names / 1,010,204 logical bytes at runner close, one operation root. These are snapshots, not physical I/O or performance evidence.

## Bounded driver repair and authorized continuation

Only the reserved regular single-link zero-length guard uses its observed zero length to derive the empty digest, avoiding reads of its locked range. Identity/size/mode/mtime checks remain. Other file hashing is unchanged.

The exact private continuation verifies prior source/case boundary, real helper-held output, no prior competing public apply or interruption, and equality of all ten engine file raw hashes. It retains the actual cb41982d development candidate and installed fixture; it does not rebuild or reapply successful setup. A fresh engine pin names the new clean driver commit. Candidate source and engine source are reported separately (the accepted API permits distinct immutable sources). A fresh no-op plan admits the real exclusion observation on the unchanged prior fixture. Interruption uses a new exclusive project/recovery child and that current pin.

```text
python -I -B tests/framework_next/test_native_windows.py --resume-tail F:/framework-next/p7-runs/native-w01/382 C:/Github/YuChia/ai-collaboration-prompts-dotnet-backend/.dev/ai-context/local/p7/n382/observations/cb3d1c6c
```

This one-task continuation is hard-bound to the observed driver failure. Its result selection contains only writer-exclusion and interruption-recovery; it cannot print a fabricated six-case single-run pass. A missed interruption remains non-pass; no retry-until-green. Fresh apply and later incomplete-operation recovery remain separate from the completed earlier operation. Original outputs and transcripts are preserved. This is the expressly permitted driver-fix continuation after material change, not a third unchanged full-suite retry.

Pending: clean commit and actual tail execution; no result claimed yet.
