# Issue 382 bounded native maintenance

[Live Issue](https://github.com/YuChia-Wei/ai-collaboration-framework/issues/382) was read with zero comments, open, updated 2026-09-23T14:26:28Z. The coordinator assignment and U001 select this implementation and the native observations. The saved C: project is bootstrap only.

Owner: local-change-implementer, framework-source mode. One independent declared GPT-6 Astra / ultra conversation; no subagents or callbacks. Root/branch/start were verified clean at `031233202f21e0793c9667cac872375d65e52be0`.

Own only new native test driver, run.py native arm, concise test README and this workflow. All product/bootstrap/distribution/packages/profiles/policy files remain read-only. Do not consume moving #381 changes.

1. Implement bounded driver and direct syntax/argument checks.
2. Validate exact commit message, commit coherent driver, verify clean source.
3. Execute the exact selected command once on that immutable commit. Product failure stops dependent cases; any missed interruption is a visible non-pass and never retried until green.
4. Preserve failed residue, pin, actual requests/results/counts and commit truthful records for coordinator handoff.

Command:

```text
python -I -B tests/framework_next/run.py --layer native-windows --native-root F:/framework-next/p7-runs/native-w01/382
```

Provision only that explicit native parent plus C: `.dev/ai-context/local/p7/n382/recovery` and `observations`. Direct/no-reparse/volume identity, disjointness and durable ignore/untracked checks are repeated by the driver. Process-termination only; OS/storage remain available. Preserve all previous runs and recovery data. No global temp modification or filesystem fallback.

Caps: two simultaneous owned children; 30 public/helper launches; three durable operations; 256 observed created names; 16 MiB retained logical bytes. Nested launches and transient creations are explicitly unavailable where not instrumented. No performance claims. Expected selected normal path is under 120 seconds; this is neither a full profile nor matrix. Each child has a bounded wait.

U001 adapts the existing workflow/task templates proportionally. Effective-rule packets, formal audit/lease/handoff machinery, old test matrices and hosted gates remain **deferred-by-owner**, owner program #322 coordinator / P7. Direct parsing does not establish schema compliance. Native evidence cannot establish target readiness or whole-P7 completion.

Graph indexed in fast mode for `framework-next-382` at the clean start; it excludes `src/tools`/`tools`, so exact tracked source reads were used for bootstrap/build entry points. Graph searches/snippets are discovery; direct source confirmed material behavior. No graph absence claim.

Preparation limits retained: initial `gh issue view` failed through blocked local proxy; the GitHub connector then supplied the actual live Issue. Two guessed documentation/tool paths were absent; tracked path listing located `p7-execution-selection.md` and `tools/build-development.py`. Initial long file-write command failed with Windows error 206 before execution; split writes succeeded. None is native product execution.

Read [report.md](report.md) and [task.json](task.json) for execution state and next action. Local checkpoints only; no push, PR, provider mutation, CI restoration, release/tag/publication or target activation.

## Completed bounded attempt

Executed clean a9af5fe793f938ac9b66f94034bda8a2cafb50ec once. Build and inspect succeeded; plan failed with protected-path WinError 1. No dependent public run was retried. Local task completes with N382-F01 owner handoff and incomplete native acceptance; see report and durable evidence.
