# P2 code integration

This is coordinator source/content inspection under U001, not an independent audit or runtime acceptance. No Lesson CLI, builder, package, test, migration, install or hosted verification was executed.

## Subjects and interface

- #330: `7224a57387ca181e7a940ff72f48b79708668d3e`, one local commit, clean worktree observed.
- #331: `4b0c985cacc10b2eca174e09892c7a31c5051d3c`, one local commit, clean worktree observed.
- Both start from `a1d8b750b8c81fd17697a39e04b55c35c4f3c4dc`; original commits remain traceable after integration.
- Direct parsing of the two committed YAML inputs confirms the same eight package members: SKILL.md, skill-package.yaml, references/configuration.md, references/operations.md, references/example.md, schemas/lesson-record.schema.json, templates/lesson.md, scripts/lesson.py. Metadata version is 1; skill identity is lesson@0.1.0. No helper or new metadata member field.
- Lesson uses one explicit JSON request with project/package roots. Assembly reads exact committed blobs and declared closure, generates installed-resource references, and requires explicitly selected scratch/output roots. The builder never imports or runs Lesson.

## Inspection coverage and limits

Coordinator read the selected product source: Lesson configuration/bindings, record/query/write/render paths; distribution parser, source selection, resource checks, runtime projection and new-directory assembly. Scope, actual delivery HEADs and clean status were read back. Direct JSON/YAML comparison and Git diff inspection are static evidence only.

Code discovery used a fresh non-persisted fast graph for #331. Its tools directory was excluded, so the exact CLI file was read directly. #330 indexing explicitly excluded src/skills/lesson/scripts, so its known tracked script was read directly with an AST function inventory. No missing graph result was treated as absence of code. No graph artifact was added to the delivery.

No new material correction was identified in this bounded content inspection. This statement is not an exhaustive defect-free verdict. The deferred P7 run must establish actual behavior and decide proportionate checks from final implementation.

## Explicit implementation limits carried forward

- Lesson supports one cooperating writer. External editors ignoring the lock, power-loss durability, migration and automatic stale-lock recovery are outside its current guarantees.
- Default notes/lessons creates no broader write authority: the notes parent must already exist or be explicitly authorized. This first-use friction is visible in the configuration contract; P7 should evaluate it rather than silently widening project write roots.
- Writes are restricted to the documented local platform/filesystem set. JSON/YAML readability and AST parsing do not prove that platform behavior.
- Candidate assembly supports the selected immutable source and exact allowlist; no installed lock, apply/delete/recovery engine, release or stable artifact is generated by this code-delivery work. Windows executable mode remains explicit inventory information.
- The two implementations each parse their own boundary. P3/P5 must deliberately reconcile configuration generalization and any justified shared primitive; do not import another skill's private script or introduce a helper field silently.
- Failed candidate directories are retained. P7 should assess bounded I/O and failure reporting; this does not authorize a cleanup engine or changing global TEMP/TMP.

## State and next work

P2 source/code delivery is locally complete; online integration is next at this checkpoint. Issue completion is limited to code/document scope. Program #322 remains open. P3 knowledge lifecycle and PR/local-backlog capabilities follow; managed apply/recovery and cutover stay P6, actual tool/build/install trials and CI review stay P7. Existing root runtime and historical project records remain active until the explicit later cutover.
