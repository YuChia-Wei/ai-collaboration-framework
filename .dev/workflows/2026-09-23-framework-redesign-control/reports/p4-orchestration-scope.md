# P4 workflow orchestration scope

Program #322 / Issue #341, selected under U001. This starts a bounded contract checkpoint while #334 knowledge source is active and #337 final package mappings await the actual package set. P1/P2 public contracts and the reconciled P3 interfaces are sufficient design inputs; product implementation waits for coordinator reconciliation with actual packages. This is sequencing, not owner reapproval or runtime acceptance.

## Selected result and ownership

One independently selectable organizational skill owns workflow schemas, tools, templates, configured filesystem stores and resume/retrospective behavior. Intended source root is src/skills/software-development-orchestrator/; confirm its public identity at the checkpoint. First-stage writes are limited to .dev/design/framework-next/workflow-orchestration/ and .dev/workflows/2026-09-23-workflow-orchestration/. Coordinator retains shared contracts, indexes, source mapping, first push and integration.

The contract must make minimal semantic inputs produce a coherent record and usable continuation. Show tracked-intent and ignored-intent stores without Git edits, actual durability claims or fixed .dev paths. Preserve task dependencies, failures, deferred verification, unresolved decisions and concrete next action. Distinguish caller assertions from observed execution. Avoid reproducing mandatory multi-file locators, indexes and audit receipts as the new product.

Retrospective may conclude no new knowledge. Public composition or concrete handoff candidates connect to Lesson, ADR, standards promotion, PR and backlog; specialists remain independently usable. Do not create their artifacts through private imports or interpret a package/record/flag as authority. No automatic normative adoption or provider mutation.

Retention settings and preview must protect active work and unresolved references, name the evidence needed for compaction/archive/purge, and preserve useful resume summaries. No automatic deletion, Git history rewrite, external archive implementation, generic scheduling engine or global reference registry. Unknown external references cannot be proved absent by scanning one store. The source scope and exact public operations follow reconciliation.

## Existing work and verification

Live title search and read-back found #316 open. It owns legacy AI-context workflow/assessment bundle authoring with existing validators and guarded multi-file updates. #341 owns the new portable workflow capability; it neither fulfills nor closes #316. P5/P7 must explicitly disposition surviving overlap, rather than silently carrying old gates into the new package.

One independent gpt-6-astra / ultra conversation, F:/framework-next/341, no sub-agents or child-created tasks. Use ai-context-governance for source coordination under U001. Return the local design checkpoint before product work; then continue the same task after coordinator selection. Preserve all source evidence and explicit unsupported choices.

Only direct content/syntax/reference/Git and the exact planned message check run now. Product CLI/help, schema validation, tests, fixtures, build/package/install/migration trials, audit machinery and CI remain deferred-by-owner to program #322/P7. No release, credentials, root activation or downstream adoption.
