# Five engineering method packages: source integration

Issue #347 returned commit `814fd12822bb270f17edbfe91d0864ba07c1aacb`
on `codex/2026-09-23-engineering-methods` in `F:/framework-next/347`.
The worktree was clean at that exact HEAD and the original task was idle.
Its source-only workflow is completed; provider integration and verification
are separate. This is coordinator content inspection, not independent audit
or runtime acceptance.

## Actual delivered scope

The 29 added files are confined to five assigned skill directories, two own
design files and four own workflow files. The source has five separately
selectable version 0.1.0 packages, metadata integer 3, explicit null
configuration, empty skill dependencies and empty artifact roles, schemas,
templates and tools. The seven operations are instructions, with no fabricated
executable or required Python runtime.

| Package | Operations | Actual members |
| --- | --- | --- |
| diagnostic-analyst | diagnose | 3 |
| ddd-ca-hex-architect | design, review | 4 |
| bdd-gwt-test-designer | design, review | 4 |
| local-change-implementer | implement | 3 |
| slice-implementer | implement | 9 |

[Design and extraction bindings](../../../design/framework-next/engineering-methods/README.md)
and [exact source inventory](../../../design/framework-next/engineering-methods/delivery.json)
retain the actual members and source rationale. Legacy source remains intact.
No shared manifest/profile/loader/adapter, root route or other package changed.

## Content inspection and limited checks

The coordinator read the fixed Git blobs for all skill entries and contained
methods, plus metadata, inventory and workflow outcomes. The content retains
diagnostic falsifiers and observation limits; architecture alternatives,
invariants and fixed-artifact review; observable GWT with independent expected
values; local semantic radius; and command/query/reactor/generic slice ownership.
Implementation methods consume explicit target authority, commands and paths.
Optional handoffs do not require another installed package or duplicate approval.
Generic implementation does not activate a role registry or .NET baseline.
No material source correction was identified in this bounded inspection.

Direct inline Python used only stdlib file/Git/hash/text functions and PyYAML:
29 UTF-8 files, six YAML documents, two JSON documents, five frontmatter
documents and 33 local Markdown links. All 29 clean-worktree files equaled
their fixed Git blobs. The 23 resource/member sets equal Git-tracked package
members and every inventory SHA-256 matches. All seven operation references
are declared members. `git diff --check 4cda6689..814fd128` completed without
whitespace errors. These checks neither import nor execute product code.

The worker's [report](../../2026-09-23-engineering-methods/reports/remediation-report.md)
retains its earlier authorization refusals, accepted resumed writes, corrected
staged EOF warning and exact planned commit-message result. None is erased by
this later source handoff. App history retrieval returned an idle task but no
message items; completion is grounded in the fixed source/workflow and clean
Git read-back, not an invented final-message quotation.

## Integration and remaining dependencies

Select this coherent source commit for online PR integration and bounded
#347 Issue completion. Preserve the original owner-confirmation checkpoint
`4cda6689bf469a2273e14e237a0c3bf7ad4b6eed` and #346 record-only checkpoint
`7fab3ffd1698e2eb8ef791d6be7748f151506642`; their referenced histories are not
rewritten. The coordinator adds only its own continuation records and workflow
discovery row around that source merge.

#346's original four-file mapping remains blocked by automatic approval review;
see [the observed confirmation outcome](p5-owner-confirmed-resume.md). Do not
substitute writers, retry without the required direct executor-task input, or
treat #347 delivery as approval for a broader mapping. The nine-package first
handoff remains unchanged. A later exact handoff for these five packages and
the already delivered structured/optional packages is still required.

U001 assigns product CLI/help/import, schema validation, tests/fixtures,
build/render/package/install/migration/probes, independent audit/lease/acceptance
machinery and CI to **deferred-by-owner**, program #322 coordinator / P7.
P7 begins only after remaining source/mapping dependencies are resolved.
Root adoption, pipeline restoration, publication and assessment-finding closure
have not occurred. Read back PR/main and Issue/Project state after the online
merge; an empty checks list is not a CI pass.
