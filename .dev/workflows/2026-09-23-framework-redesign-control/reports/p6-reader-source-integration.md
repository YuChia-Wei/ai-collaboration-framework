# P6 reader and planner source integration

Coordinator inspection of #354 `8be2f8ab807764cdd691cf81b537811c30bc64f7`, from base `5986b2146bb4559609acb0d5c7d09e8570375ea8`. Task `01a0cc0b-1bc5-76a0-a95a-090159fa3010` completed; assigned F:/framework-next/354 was read back clean at that commit. The eight changed paths are two owned source modules and their own design/workflow records.

Read the full new reader/planner source through graph snippets and fixed-commit AST/content extraction, plus the API handoff and source report. Compared candidate identity and emitted field shapes with the actual assembly producer. The reader uses the existing metadata 1/2/3 loader/reference owner; neither builder nor product operations were invoked. Scope includes exact candidate closure, all-old inventory observations, no inferred unowned adoption, bounded unknown preservation, direct byte-only protected paths, closed requests/results, explicit engine identity, roots/path budget, no-op and mode-only distinctions. Planning retains pending mutation prerequisites and project_readiness=not-assessed. No material source finding requiring a repair was identified in this inspection; behavior remains unverified.

Discovery: fresh fast nonpersistent `framework-p6-354-review` index on the selected distribution directory returned 150 nodes/901 edges but no commit attestation. An initial file-pattern query returned no nodes; this was not absence evidence. A broader graph output was verbose, so bounded qualified-name/function queries and direct fixed-commit extraction supplied the material observations. Both worktree source files exactly matched their fixed Git bytes: installation_state.py SHA-256 `676c9612bb52434ed56944445a3b4e33b13e2553c6ab1c582e48a2511c19f290`; installation_plan.py `76ecca4874d2dca290e9eabc2d36ac4e939eef9eefc940edb04caf222ff7aa6d`. Graph edges guided discovery, not exclusive dependency or absence claims.

The [actual source handoff](../../../design/framework-next/installation-planning/handoff.md) defines six current reader-engine files, mutable ephemeral dataclasses, exact inspect/plan boundaries and fixed limits. These are sufficient for this read-only source slice, not the complete mutation engine. #359 is the sole successor for necessary integration of these files with coordination/bootstrap/native I/O/apply/recover. It must close the complete execution file set, resolve mechanical prerequisites truthfully and preserve the public state distinctions. No source schema without a real owner/reader is introduced; these exact JSON shapes are owned by the actual Python readers and documented contract.

Worker actual evidence: eight UTF-8 files, two AST parses, direct workflow JSON/YAML, 13 local links, staged scope/diff and full message checks. Retained prior proxy refusal, nonexistent exploratory filename and Windows command-length error are preparation failures, not product test failures or erased history. Coordinator combined direct checks are recorded before integration commit. All product CLI/imports, schema validation, tests/fixtures, builds/packages/install/migration/backend trials, audit/lease/acceptance packets and CI remain **deferred-by-owner**, U001, program #322 coordinator / P7.

This source integration does not install, activate, publish, migrate configs or establish native durability. Full framework findings stay partially addressed. Shared mapping #346 and engineering source #347 remain under their existing automatic-approval blocker; this source unit does not replace their writes.

Combined integration direct read: 30 UTF-8 files, 8 JSON, 5 YAML, 2 Python ASTs and 49 existing local Markdown targets. Exactly three members per optional package and actual instruction targets were read back. `git diff --check HEAD` passed. No product import, schema validation or behavioral execution.
