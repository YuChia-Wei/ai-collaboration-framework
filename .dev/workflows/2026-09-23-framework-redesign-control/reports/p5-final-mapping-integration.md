# Final mapping integration and P7 design entry

The original #346 task returned final mapping
`4ffa3881484783a2e35152ad426bec359d2d9252` from a clean
`F:/framework-next/346`, branch `codex/2026-09-23-instruction-operations`.
The original task was idle at handoff. Its bounded source/mapping workflow is
completed; no provider closure, product invocation or P7 acceptance is implied.

## Fixed-source inspection

The coordinator read the five product-file diff and owned design/handoff.
Direct Git/YAML comparison confirmed 18 exact component closures/113 members;
all member sources equal the actual tracked package files and all destinations
preserve the relative path under that skill's managed core directory.
The raw original nine component-row bytes, existing five profile registrations,
Codex adapter and lesson-minimal/knowledge/work-management Git blobs are preserved.
No package source, loader or adapter changed in the final mapping batch.

| Complete profile | Packages / ungenerated Codex entries | Payload members |
| --- | --- | --- |
| lesson-minimal | 1 | 9 |
| knowledge | 3 | 26 |
| work-management | 3 | 28 |
| engineering | 10 | 53 |
| collaboration | 16 | 107 |
| source-repository | 15 | 99 |
| context-maintenance | 2 | 6 |

Optional context maintenance is excluded from ordinary selections. The source
profile excludes local-backlog because this repository uses GitHub Issues.
Profiles select a complete installation; sequential application does not union
them. Selection metadata is not permission to install, remove or activate files.

The final ten-file batch passed direct UTF-8 parsing, six YAML documents, one
JSON document, 20 local Markdown links, member/destination/version/count
comparison and `git diff --check 34400e0f..4ffa3881`. The earlier first-mapping
checkpoint `444327034aa14d9a9364c72e33e77ac879e9e5df` and coordinator selection
`34400e0ff377301601be5b7e167ed9e05261830c` remain referenced commits, not squashed
away. The [worker report](../../2026-09-23-instruction-operations/reports/remediation-report.md)
retains actual message checks, admitted writes and all earlier approval refusals.

## Provider integration and next work

Select the coherent mapping/source batch for online PR integration and bounded
#341/#346 completion. #347 source is already online through PR #363. Read back
PR/main and both Issue/Project states after merge. The ignored live observation
will be `.dev/ai-context/local/framework-next-control/p5-final-mapping-readback.json`;
this locator does not assert that observation already exists.

After this source/mapping batch is online, start two independent design-only
conversations under [the P7 assignment](../../../design/framework-next/p7-design-handoff.md):
#364 owns focused verification and concrete pilot planning; #365 owns replacement
source rules and seven-pipeline dispositions. They write distinct design/workflow
paths. They neither implement/run tests nor enable CI. Coordinator reconciliation
precedes bounded implementation/trial assignments; final owner adoption still
precedes any pipeline restoration.

The current source and mappings are ready for that design step. Root adoption,
public-runtime behavior, native maintenance, actual verification and assessment
findings remain unaccepted. U001 continues to mark product/schema/tests/build/
install/migration/audit/CI as **deferred-by-owner**, program #322 coordinator / P7,
until specifically selected later execution work.

## Retained preparation observations

- `Get-Volume -DriveLetter F` was denied by the execution environment. A later
  limited `System.IO.DriveInfo('F:')` read returned NTFS, Fixed, total
  17,179,865,088 bytes and 14,222,045,184 available. This is metadata only, not a
  native-backend, RAM-loss, speed or durability test. The owner identifies F: as
  the RAM disk; `F:/ai-context-tests` remains excluded.
- Automatic approval rejected a combined local fast-forward and Project-field
  command before execution, citing unverified option meanings/authority for
  Status, Target release and Owner review. The expressly bounded local
  fast-forward was then performed separately. Live field read-back established
  names/IDs; a narrower request added #364/#365 with **Status=Planned only** and
  was accepted. Target release and Owner review were not mutated or claimed
  approved. No bypass or alternate writer was used.
- A guessed `.dev/standards/GITHUB-WORK-MANAGEMENT-POLICY.md` path did not exist;
  the actual YAML policy and its named SOURCE-WORK-MANAGEMENT-AUTHORITY.md were
  read. Newly created #364/#365 received the required source attribution marker
  and additive `created-by:codex` label after their initial creation.
