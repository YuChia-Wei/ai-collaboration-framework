# P5 instruction source integration

Coordinator inspection under U001; not independent review or executed verification.

## Exact source and bounded result

- #346: `4795f7c1c6751a1702042ef76276f355e4ed7c37`, clean F:/framework-next/346; twelve changed paths. Metadata-v3 closed instruction/tool union, restricted null configuration, direct Codex projection consumers and the three-member code-reviewer package. Shared manifest/profile mapping remains pending in the same Issue/task.
- #348: `b6ba669ff465bb6c6429bcc24cdf9b37af0ce964`, clean F:/framework-next/348; sixteen added paths. requirement-author has four members; spec-author seven. Both are metadata 3, package 0.1.0, draft/normalize instruction operations and null configuration. Source scope is complete; mapping belongs to the shared writer.
- Both start from `842b73ca09d701d1561109255193d80439dc996b`; their referenced commits are preserved. Integration base is `b64ade171d04811449b94c6e285634b07c17151b` (PR #350).

The coordinator read all changed loader/adapter/consumer paths and all three package methods/default templates. The union rejects both/neither arms through the existing closed mapping helper; only tool operations enter the tool-operation equality check. Null config requires empty roles/schemas/templates and the adapter emits no config/store provisioning instructions. V1/v2 retain their tool shape and configuration branch. These are source observations, not behavior/compatibility results.

Fresh scoped graph `framework-p5-346-review` contains 82 nodes/441 edges. The selected inbound route is project_entry <- select <- assemble. Graph results have no exact-SHA attestation, so fixed Git diff/source/AST read-back bounds the observations. Actual source and declared member sets are compared directly; no builder, renderer or package loader is invoked. Common reviewer explicitly reports absent .NET specialist coverage. Authoring preserves target input/template/destination, uncertainty and source authority, and does not invent a mandatory stage chain.

No material discrepancy was identified in the inspected changes. P7 still needs actual union rejection/acceptance, v1/v2 behavior, null-config rendering, package closure and useful target-use checks. No source package is installed or activated here.

## Other work and failures

#341 source is online via PR #350, merge `b64ade171d04811449b94c6e285634b07c17151b`; provider read-back at 09:52:25 +08:00 reported Issue OPEN/Project In progress and empty PR checks. Keep it open until actual mapping. #342 is already CLOSED/Done through PR #349.

#347 is interrupted before writes: automatic approval review twice rejected its assigned F: package writes because delegated/tool-read authorization was not accepted as a direct user instruction. No source or commit was created. The coordinator requested direct confirmation and continues unaffected work; do not retry its dependent writes without that answer. #346/#348 callback sends were also rejected. The coordinator read their completed task results and local Git records directly; no callback success is claimed and no resend is needed.

Coordinator preparation read errors (wrong guessed capability-map/source-handoff name and #345 design read in the main checkout before integration) were read-only missing-path errors. Actual tracked paths were then read. Worker proxy, command-length and orchestration preparation failures remain in their reports. None is a product validation result.

#345 returned proposed design `51229b63565ce6e836d57a4b107cf6df5554bf7c`. The coordinator requested revision in the same task: explicit quiescent maintenance, held lock for participating maintenance writers only, no mandatory skill invocation launcher, and managed-byte consistency separate from project/capability readiness. This revision is pending; the original design is not adopted as a shipped engine.

## Next ownership

The coordinator owns online source integration, receives #347 when unblocked, and supplies one concrete actual-source mapping handoff to #346. #351 designs frame/compliance and #352 optional context maintenance; neither owns src or activation. Product CLI/help, schema validation, tests/fixtures, package/install/migration/compatibility, audit/lease machinery and CI remain **deferred-by-owner** under U001, owner program #322 coordinator / P7. Direct readability/syntax/reference/Git/diff and exact planned-message checks alone are allowed now.

## Coordinator direct checks

Direct changed-file read-back: 41 UTF-8 files, nine JSON, six YAML, three Python ASTs and 73 existing local Markdown references; zero missing references. Actual package sets are 3/4/7 members. Fixed #346 package.py bytes match the integrated file; direct AST caller inventory names selection.py. Both working/staged diff checks were clean. These checks invoke no product code. The coordinator also corrected a Markdown table row gap before commit.

The first local integration commit left the uppercase tracked INDEX.MD unstaged because the pathspec used lowercase. Before dispatch or push, the coordinator staged the exact tracked path and amended the unshared integration commit with the already-validated unchanged message. Original worker checkpoints remain unchanged.
