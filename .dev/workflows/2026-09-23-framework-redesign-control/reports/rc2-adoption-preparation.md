# RC.2 source and target adoption preparation

This is preparation from immutable source 81c9603b6b5ceae7b0e33a541bc12e28118bc6be and a fresh read-only mq-lab observation. It does not adopt a framework, execute a runtime, grant a gate pass or retire a route. S3 #406 supplies the exact verified candidate/engine and API-2 transition contract; S5 consumer #407 supplies optional task-scoped instructions. Actual source and target adoption remain separate assignments.

## Source routing inventory

The accepted [source capability inventory](../../../design/framework-next/rc2-contracts/source-capabilities.json) contains 21 identities, 16 existing legacy routes, 18 delivered packages and 13 overlaps. Both tracked runtime README inventories list the same 16 legacy skills. A bounded read-only analysis checked the requested inventory, wrappers and root routing at the fixed source commit.

| Category | Identities | Proposed treatment, pending concrete transition |
| --- | --- | --- |
| Full successor for selected operations | bdd-gwt-test-designer, code-reviewer, ddd-ca-hex-architect, diagnostic-analyst, local-change-implementer, requirement-author, slice-implementer, spec-author | Withdraw the corresponding old unprefixed discovery entries only together with admitted managed aicf entries and exact project-owned route edits. |
| Split source/target duties | ai-context-auditor, ai-context-governance, problem-frame-author, software-development-orchestrator, spec-compliance-validator | Narrow the old route to its retained duty and name the new package's exact operations. No duplicate active route for an operation. |
| New delivered identity | adr, lesson, local-backlog, pr, standards-promotion | New managed entry; availability alone does not configure a store, import old records or enable decisions. |
| Legacy owner retained | ai-context-init, ai-context-upgrader, ai-context-release-closeout | Preserve published-format initialization/upgrade/recovery and source historical release duties; no new package successor. |

Split duties are explicit: legacy assessment persistence/terminal work; source governance, policy, release and customization reconciliation; legacy CBF/SWF intake and active records; existing workflow artifacts, #322 and Issue authority; and the target-selected .NET gate/rule resolver. The current inventory is not a complete predicate map for broad legacy triggers. The source-adoption owner must reconcile exact predicates and after-bytes before withdrawing the five mixed routes; this is coordinator-owned reconciliation within the selected scope, not a new blanket approval gate.

The project-owned intent set is the exact changed subset of old .agents/.claude SKILL entries for the 13 overlaps, three associated agents/openai.yaml files where discovery changes, both runtime README inventories, AGENTS.md with its aligned translation, and the canonical legacy registry if routing needs an explicit split. CLAUDE.md needs no change while its thin AGENTS pointer remains correct. Select all 18 delivered IDs, zero knowledge packages and explicit adapters in installation.json only against a real verified catalog identity. Existing framework.json changes require an actual selected configuration need; none is inferred from installation.

Do not hand-edit framework.lock or generated aicf entries. Old unprefixed wrappers are project-owned, so managed prefix migration cannot remove them. Submit closed project-edit intents with exact before bytes/absence and after objects to S3; paired recovery must restore old discovery and root routing with managed state. No transition has run.

## mq-lab retained input observation

The read-only target observation still resolves to cc0e345367a1d24f49bf8fa68e0eb668df9e5e55, the S1 fixed baseline. Tracked status is clean; untracked/ignored cleanliness was not assessed. AGENTS.md, CURRENT-FRAMEWORK.md, framework-binding.json, effective-rules.yaml, customizations.yaml and the installed lock match their exact Git bytes. The binding records 14 rule IDs, four customization IDs and 20 route rows; its state is pilot-review. This observation does not re-establish independent admission.

The retained lock SHA-256 is a04e2df01f5f6df6889b7c09daa1b9ae1c78e6b503aac852d5aa2b325c56a0e8. Rule and customization meaning remains target-owned, including Inventory EF Core/Npgsql, Products/Orders Dapper and Orders-selected event sourcing. The selected target command and separate independent review remain mandatory under target authority. Source U001 does not waive either.

## Execution and pending interfaces

S5 consumers use five fixed skill roots and optional metadata-4 declarations. S3 owns the verified selected resource/index and binding observations; S5 owns task applicability and coverage reporting. The interface must be published before final consumer acceptance, without a second parser or implicit configuration writes.

Four authored adapter cases exist in test_rc2_adapters.py. The current contracts runner imports only test_contracts.py, so those new case names are not runnable through that arm unchanged. S6 must choose an exact new-module launcher/runner after the concrete implementation stabilizes; no full suite is selected. A requested graph refresh still returned the prior d8e59eae branch identity, so direct tracked-file inspection was used for this bounded runner fact. No test, actual resource loading, installation, recovery, Codex/Claude discovery or target gate ran in this preparation.

All U001 legacy/formal/hosted/CI outcomes remain deferred-by-owner to program #322 coordinator/P7. R1-R8 remain in the [acceptance status](rc2-acceptance-status.md).
