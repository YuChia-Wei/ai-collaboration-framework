# P5 design reconciliation

Received #342 at `66f353393b3f7ef59265d6c38b119a832e8113a9`, based on `94741016bd7baae9936bb77dd6c4f56d37036e97`, clean F:/framework-next/342. Nineteen changed files are confined to its own design/workflow roots. [Selected decisions and grouped source responsibilities](../../../design/framework-next/p5-selected-contract.md) authorize the next bounded source work; no implementation is attributed to #342.

Coordinator inspected the operation model, implementation slices, capability dispositions, format ownership and exact metadata specimen. The two staged proposal documents first read for planning were subsequently compared by Git blob to the delivered commit and matched. Fixed-commit direct JSON comparison confirmed all 95 original registry rows/IDs are retained unchanged, each resolves to one of 17 complete ownership groups, all 16 canonical skill IDs are covered, and all five actual package member sets total 44 paths/six schema resources. The ADR metadata specimen is byte-identical to the actual source. These are inventory/content facts, not schema acceptance or executed coverage.

The selected metadata-v3 union resolves the real loader requirement for every operation to name a tool without fabricating executables for qualitative work. Three implementation batches preserve eight specialist identities: one shared loader/reviewer vertical slice, five engineering methods and two authoring skills. Shared mapping remains one writer and follows actual package source. Structured frame/compliance and optional context maintenance remain explicit later work; the design does not silently remove them from program scope.

M01 is selected only as a conditional P6 design input for actual closed JSON v1 projects needing v2. It does not justify rewriting current defaults/v2 projects or legacy records. #345 receives the exact decision after this checkpoint. Legacy transaction recovery, source release history, provider evidence and ordinary prose retain their distinct owners. No runtime registry or migration engine was added.

P4 design is already online via PR #344; #341 source continuation and #345 design runtime/F: identities were read back and retained in coordinator dispatch evidence. P5 design online integration and bounded source Issue creation follow. Product execution/schema/tests/CI remain deferred-by-owner under U001 to program #322 coordinator / P7.

Actual coordinator checkpoint checks: 32 changed/new documents read as UTF-8; 15 JSON and 4 YAML documents parsed directly; 355 local Markdown links resolved; staged/unstaged whitespace and exact planned merge-message checks completed. These are static content/syntax observations only. #346/#347/#348 Project items were read back OPEN/In progress, release Unassigned and Owner review Approved (execution authority, not acceptance).
