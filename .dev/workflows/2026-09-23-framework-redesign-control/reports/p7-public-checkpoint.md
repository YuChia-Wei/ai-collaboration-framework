# Public skill checkpoint: partial execution

The coordinator received Issue 373 at
`7996b32d3d4f70553b203299e25dc69d9413ff9d`, based on
`f8f0d073e6442df8a280c5941a2c7a1d6ed7060b`. The assigned
`F:/framework-next/373` worktree was clean. The 43 changed paths comprise five
public test/runner/documentation files and 38 issue-owned records. Exact Git
comparison confirms no product, schema, metadata, shared helper, contracts test
or engine test changes. The original checkpoint is preserved.

## Evidence and limits

[The executor report](../../2026-09-23-public-skill-checks/report.md) retains
every attempt, exact refusal streams and final-byte limitations. There is no
complete T1-T7 family pass. Lesson, ADR, promotion, workflow and CBF completed
independent C4/C6 read-only assertions. All nine allocated runs returned nonzero;
read-only skips are explicitly incomplete. Backlog was not invoked after the
shared committed-metadata/parser incompatibility was established from source.

Actual Lesson create refused `unsupported / filesystem` without mutation;
actual PR explain refused `invalid-input / yaml-token`. ADR and CBF expectation
mistakes were corrected in tests, retaining the original failed attempts.
Later test-body changes received syntax/source inspection only; earlier runs
do not become final-byte passing evidence. No provider simulation, complete
round-trip, selected large-input case, candidate assembly or installation passed.

Across nine retained fixture roots, the executor observed 98 public Python and
36 helper Git launches, 136 files and 1,605,714 retained logical bytes. Maximum
observed logical bytes were 1,610,634; helper-authored bytes were 78,672. Capture
driver launches and unavailable opaque grandchildren remain separately labelled.
These counts do not measure SSD writes, performance or token savings. No failed
fixture was removed. The tracked raw streams preserve necessary evidence beyond
volatile RAM storage.

[Coordinator OS inventory](p7-public-volume-observation.md) distinguishes the
F: path/volume API observations from public-tool outcomes. Direct drive-root
inventory reports NTFS, while the actual returned directory query failed; strict
canonical resolution also failed. This is insufficient to approve a path
fallback, weaken guards, diagnose a driver defect or establish native acceptance.

## Integration and next owners

The original Issue 369 conversation is assigned the actual public caller
binding from this checkpoint. It may change its gate/result parser, focused
synthetic tests and own records only. It must reject partial/skipped/mismatched
or absent execution and cleanup evidence. No public writer rerun or policy
write is assigned. Native dispatch remains unavailable.

Issues 368, 369 and 373 remain open. Automatic approval review stopped the
original four-file path/metadata repair and prospective policy/root/template
batch for insufficient trusted write authorization. Required direct replies in
their original tasks are still pending; the caller binding is a separately
permitted responsibility, not a replacement writer for those stopped actions.
Volume-guard repair across skill tools is outside the four-file repair scope.

After the blocked source repairs and a supported F: path/volume disposition,
select only affected fresh public runs. Native N1-N5, first Lesson pilot, root
adoption and CI restoration remain outstanding. The prior incomplete public
installer request cannot supply positive N1 evidence. No silent C: fallback,
RAM-disk reconfiguration, release or downstream rollout is selected.

All unselected legacy/full/history/upgrade checks and audit/lease machinery
remain `deferred-by-owner` under U001; owner is program 322 coordinator / P7.
Repository Actions and all nine workflows remain disabled at the latest PR 374
read-back; fresh provider state is required before the next online checkpoint.
