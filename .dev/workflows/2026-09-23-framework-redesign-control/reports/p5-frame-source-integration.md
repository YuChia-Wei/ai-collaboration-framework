# Frame and compliance source integration

Coordinator inspection of #356 `7038ee97f809f359d776f5d74b4959e51dbe518d`, based on `59877b8d2f61e9a95615ea95d597fe35da2f44cf`. Task `01a0cc18-b555-7022-a8fa-51456958e542` completed and F:/framework-next/356 was clean at the exact commit. Its 21 paths remain inside the four assigned roots: ten frame members, six compliance members and five own design/workflow files.

Read all package entries/metadata/methods, schema fields/definitions, template/example, the complete tool via scoped graph snippets and fixed-commit AST extraction, plus delivery handoff/report. The tool has one owner for strict JSON/schema/typed references/full criterion inventory, required request operation, configurable constrained stores/templates, create-only publication and truthful none/published/uncertain results. It reports every unresolved statement/assertion and requires question links. The public structure-result is an ephemeral operation contract, not another persisted family. Compliance consumes that actual result without a parser duplicate, separates structure/semantics/runtime, prioritizes contradictions, refuses empty/missing required evidence and exposes a .NET instruction profile only by explicit selection. No material content finding requiring repair was identified; this is not an independent behavioral audit.

Graph discovery first indexed the package with scripts excluded, so a second nonpersistent index targeted the exact scripts directory (67 nodes/258 edges). Neither index returned a commit attestation. Fixed Git content and direct worktree comparison bound the reviewed script: SHA-256 `7be80366c77ae390d8659e26ee18307c5967f47f47c51316573f7a3578019cf8`, exact raw equality at the selected commit. Source/AST reads did not execute or import the product.

Observed source limits remain explicit: 4 MiB/32 nesting/1024 array/16384 text/96 identifier; existing ancestor directories; flat explicit references; NTFS or selected Linux local hard-link backend; no overwrite/mkdir, power-loss guarantee, migration or SWF machine reader. Actual path/backend/publication/cleanup behavior remains unproven. Directory observations and rechecks are not a hostile-writer sandbox or a measured I/O improvement. Prior records and semantic loss notes remain preserved.

Worker recorded 21 UTF-8 files, two JSON, three YAML, one AST, 31 local links, member/reference closure, staged scope/diff and exact message checks. Its proxy failure and static-source repairs are retained in its report. Coordinator integration checks will record actual results before commit. Product CLI/help/import, schema validation, fixtures/tests/build/package/install/migration/backend probes, audits/leases/acceptance packets and CI remain **deferred-by-owner**, U001, program #322 coordinator / P7.

Exact shared mapping still belongs to its separately assigned owner; no #346/#347 blocked action is replaced. This source delivery does not activate routes, resolve #316/#317/#318, publish a release or establish runtime acceptance. #359 continues independently from its existing fixed source checkpoint.

Final combined PR-range direct checks at 2026-09-23T10:55:11+08:00: 55 UTF-8 files, 12 JSON, 8 YAML, 3 ASTs, 104 existing local links; exact 10/6 members and declared operation/resource paths read back; `git diff --check origin/main` passed. No product or schema execution.
