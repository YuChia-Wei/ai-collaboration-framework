# Original-request coverage at P7 entry

Coordinator reconciliation for program #322. Source baseline: PR #366 /
`171f33474f88888fbe853600de04bfe9c5716b25`; design dispatch checkpoint:
`9647feab5240ff10ed20d135acfa2d037c114f10`. This is a scope/status map, not
product verification or a new expansion of accepted work. [U001 and D01-D07](../../../assessments/ASM-20260923-00-6oq/execution-plan.md)
resolve the earlier proposal's sequencing; [P7 assignments](../../../design/framework-next/p7-design-handoff.md)
remain design-only until the coordinator selects actual implementation/trials.

## Framework capabilities requested

| Original item | Delivered source or selected decision | Work still required / explicit limit |
| --- | --- | --- |
| 1-1 portable skills and organizational workflow | 18 mapped packages; instruction/tool distinction; empty required/optional skill dependencies; workflow owns its record, tool, paths, tracking and retention values | Actual independent use and composition still unexecuted. Retention is on-demand preview; no scheduled compression service. |
| 1-2 project-owned .dev and configurable documents | Package configuration selects project stores/templates; authoring packages take target-owned locations and rules | Root remains on legacy routing until staged adoption. No bulk conversion of old specs/requirements. |
| 1-3 .ai core/custom ownership | Selected managed installation owns core/runtime/lock; project owns custom configuration and records | Actual custom-byte preservation, root bindings and installation behavior require P7 evidence. |
| 1-4 standardized ADR and Lesson skills | Separate ADR and Lesson packages with owned structured operations and record versions | Public create/read/query/lifecycle cases and actual use are pending. Candidate/draft is not acceptance. |
| 1-5 standardized PR skill | PR preparation package plus explicit GitHub adapter boundary | Focused actual subject checks and later authorized provider use; preparation does not authorize merge or Issue closure. |
| 1-6 workflow retrospective to retained knowledge | Workflow produces semantic handoff candidates for the owning knowledge/work-management packages | Actual composition is pending. No fabricated Lesson when a retrospective yields no useful knowledge. |
| 1-7 promote knowledge into project rules | Standards-promotion package binds sources, target and expected bytes; proposal/adoption/effect are separate | Actual promotion example and root ownership still pending; no automatic rule adoption. |
| 1-8 optional AI-context maintenance | Separate two-package context-maintenance profile; ordinary collaboration and source-repository selections exclude it | Profile selection is complete replacement, not a union. Root cutover must preserve source-only historical/release duties. |
| 1-9 schema/tool pairing and version evolution | Actual structured owners have tools; instruction-only metadata permits no artificial schema/tool; #342 retains legacy family dispositions | P7 selects direct contract/negative cases. Config-1 M01 remains unassigned without real input. No all-history migration engine. |
| 1-10 replaceable product and differential update | src manifest/profile/runtime mapping and owned differential maintenance/recovery source | No executed update/recovery or I/O savings claim. Custom and non-managed recovery remain separately owned. |
| 1-11 local-file backlog skill | Independent local-backlog package; optional for this source repo's GitHub workflow | Focused actual operation coverage pending; no automatic issue synchronization. |
| 1-12 possible future CLI/tools | Small package-owned tools are delivered; larger CLI/runtime work remains conditional P8 | Reassess against observed use, not a prerequisite for this redesign; existing Issues are not silently closed. |
| 1-13 compact/archive/remove and online storage | Workflow retention preview, useful resume summary and explicit retention safeguards | Automatic destructive cleanup, provider archival and multi-provider storage remain future scope. Original records are preserved. |

## Source-repository maintenance requested

| Original item | Delivered source or selected decision | Work still required / explicit limit |
| --- | --- | --- |
| 2-1 dogfood the framework | Concrete #361 ownership/configuration/route/adoption design | First isolated Lesson pilot, then bounded root adoption and broader ordinary capability use; root activation has not happened. |
| 2-2 isolate editable product under src | src is the sole reusable product source; root becomes a consumer with project-owned governance | Existing legacy roots remain active until each explicit cutover; do not maintain a second manual product source. |
| 2-3 packaging/reference structure | One source manifest with seven complete profiles, exact members and Codex projection | Actual selected build/install and useful use remain pending. No release, package publication or marketplace is authorized by source completion. |
| 2-4 remove expensive self-test interference | U001 stopped early tool/test execution and all hosted pipelines; #364/#365 now design focused replacements | Implement/select and exercise proportionate replacements; decide each legacy gate's disposition. Do not relocate the full old burden to nightly. |
| 2-5 configurable high-I/O target | Program worktrees use F:; P7 designs must retain zero-config defaults and explicit disposable output-root opt-in | Later preflight and contained trials; persistent recovery/backup stays separate. RAM execution is not SSD wear or crash-durability evidence. |

## Remaining sequence and completion limits

1. Reconcile #364 verification and #365 source-policy/pipeline designs together.
   Each proposes a few bounded work units; this table creates no speculative Issues.
2. Implement the selected new checks and policy/pipeline source coherently while
   hosted Actions remain disabled. Run only separately assigned focused trials.
3. Complete the isolated Lesson pilot, resolve observed failures in their owning
   source, then adopt the selected root capability and assess actual routing.
4. Extend ordinary root use only with concrete prerequisite evidence; preserve
   historical records and separately owned legacy release/recovery responsibilities.
5. Present the exact CI subset/context/settings and actual observations to the user.
   Only explicit owner adoption permits restoration; live execution is a distinct result.

The original roadmap's suggestion of a separate downstream project is not an
assigned target or downstream write authority. If later evidence needs such a
project, select its identity and scope explicitly. P8 providers/CLI/publication,
automatic cleanup and general historical migration remain conditional follow-up
work, not hidden P7 acceptance requirements.

All product runtime/schema/tests/build/install/migration/native acceptance remains
`deferred-by-owner`, U001, owner program #322 coordinator / P7, until specifically
selected. Direct source, syntax and Git observations are the only current checks.
Program findings F-01 through F-08 remain partially resolved. The [delivery ledger](remediation-report.md)
owns current disposition; old report paragraphs remain historical observations.
