# P5 remaining designs and source scope

Coordinator bounded inspection, not independent acceptance. Received clean design checkpoints #351 `214c1c1c9bc6da26c1202e7a92d7d678d54b1e3b` (13 files) and #352 `ecf27befc599aa3ee589dff36d0597f0d1c24a8b` (11 files), both from `731d658b6004110fd59224ca39aa3a5d63891d91` in their assigned F: worktrees. Original commits remain preserved.

The coordinator read both entries, format/operations/ownership/handoff documents and optional skill specimens. Direct JSON extraction inspected the draft CBF root/closed object definitions, limits and inert example fields without schema validation. [Selected capability contract](../../../design/framework-next/p5-final-capability-contract.md) adopts D351-01..05 and D352-01..06, clarifies the request operation discriminator and unresolved assertion/question link, and assigns #356/#357 source. No source package is claimed from a design specimen.

CBF supplies an accountable format owner; semantic/runtime compliance remains separate. Optional maintenance uses prose and existing project state rather than adding another assessment/ledger system. SWF, unsupported history, source authoring #316/#317/#318 and current root/active-recovery duties remain visible and independently governed. The design supports preserving these duties; it does not prove no old transaction exists or authorize a transition.

Worker actual checks: #351 13 UTF-8, 6 JSON, 1 YAML and 19 local references; #352 11 UTF-8, 2 JSON, 1 YAML, 22 local references and 17 pinned source identities. Both performed their exact message/diff checks. #352 retains its bookkeeping-helper assertion failure and corrected scope check. Command-length/proxy/path preparation failures remain in worker reports. Neither executed schema/product/tests/CI.

Coordinator merge preparation initially tried a second merge before finishing the first two-head merge; Git refused with MERGE_HEAD present. No worker commit changed. The coordinator aborted only that uncommitted integration on the clean feature branch and re-ran one three-head merge, including #346's report-only blocked checkpoint `235cfaa5e2b915ce1722dec991c3cb8904c12691`. That checkpoint modifies four own workflow files; no mapping was written.

#346's two mapping writes were rejected by automatic approval review despite live Issue and fixed handoff evidence; reason: package selection changes lacked direct user authorization accepted by that reviewer. #347's assigned source writes were rejected for the same authorization-recognition boundary. The coordinator requested a consolidated direct confirmation and does not retry, substitute a writer or silently bypass it. Both remain open/blocked; current manifest stays five packages/44 members. Proposed 68-member/five-profile mapping is not delivered.

PR #355 read-back at 10:17:33 +08:00: merged `8e4796cde7549b1f13a7a1ea13bbe738659aa98b`, main matched, #345 CLOSED/COMPLETED and Project Done. Its clean worktree was detached at the preserved design; only the merged branch was removed. #354 source continues independently. Actions/CI remain disabled and no empty check rollup is counted as passing.

Product CLI/help, schema validation, tests/fixtures, build/install/migration/compatibility, audit/lease machinery and CI are deferred-by-owner under U001, owner program #322 coordinator / P7. Source designs and administrative integration do not resolve final program findings.

Coordinator direct checks: 40 UTF-8 files, 15 JSON, 4 YAML, 82 local references; none missing. JSON schema drafts were read as data only. No product/schema execution.
