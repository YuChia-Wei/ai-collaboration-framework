# P6 maintenance source and source-adoption design integration

Coordinator content inspection under #322/U001; not an independent audit or executed product verification.

## Fixed deliveries

| Issue | Fixed local commit | Scope and disposition |
| --- | --- | --- |
| #359 | `a2e5daf71a4a830eab109370597872253df4497e` | Six Python files plus six own design/workflow files; source complete with P7 execution deferred |
| #361 | `c76f25480cc8141695ecdb9a1ad8cf5962321d11` | Five design files and six own workflow/evidence/report files; design complete, no adoption performed |

Both independent Astra Ultra tasks returned clean assigned F: worktrees and stopped before first push. Their referenced commits and the shared #361 bootstrap `b4a147c1bc8ec00fcdb92a7006c142209c61a29f` are retained. Online integration and Issue/Project state remain separate until provider read-back.

## Source inspection

Read the full new maintenance facade, native I/O, held-writer coordination and fixed entry; inspected the exact state/planner diff and selected unchanged observation helpers. The graph indexed only the distribution directory at the clean #359 subject, persistence disabled: 221 nodes / 1486 edges. It supplied no commit stamp, so fixed Git identity, exact source-byte comparison and direct AST inspection bind the actual six changed source files. An initial over-specific graph path query returned no results; a name query found the intended symbols. Absence was not inferred from that first query. The separately located fixed entry was inspected directly.

The source uses the existing candidate/package/lock readers; recovery uses the same candidate semantics and exact before/after object closure. Apply recomputes the accepted plan under native writer exclusion, captures and rereads durable objects before marker admission, verifies all old members before mutation, verifies the complete target before lock publication, and removes the marker last. First-install preparation writes affect partial-state reporting. No-op requires existing coordination and allocates no operation. Unknown data and same-byte unowned collisions are preserved. The fixed entry requires isolated Python with bytecode disabled and verifies the ten-file local engine closure; it is not source-authenticity or loaded-memory attestation.

Selected source support is Windows local fixed/RAM NTFS/ReFS or 64-bit Linux with the declared local filesystems/native primitive. Supported failure declarations cover process termination or project-volume loss with a distinct surviving recovery volume. No power-loss, OS-crash, hostile-concurrent-writer, arbitrary storage or measured durability guarantee is adopted. Installer project readiness stays `not-assessed`.

Conservative recovery limits remain explicit P7 inputs: pre-record first-install residue and a retained guard after clean-install restore require owner reconciliation; unknown/partial publication siblings are not deleted; ambiguous partial loss during a second interrupted whole-loss reconstruction is refused; a completed operation is not later rollback authority. These limits follow the selected preservation boundary and do not justify a source-only behavior-pass claim. P7 must select practical recovery/reconciliation observations before any pilot. No force overwrite, generic migration, cleanup service or new journal is added here.

## Adoption design selection

Select [#361's plan](../../../design/framework-next/source-adoption/README.md) as the concrete continuation baseline, preserving its fixed-input observations and proposal status:

- First pilot remains exactly lesson-minimal / Lesson 0.2 / Codex. New config 2 and `.dev/lessons/records` are reviewable future bindings, not applied settings. No actual M01 need has been demonstrated at the named observed paths.
- The proposed `source-repository` selection contains ten delivered ordinary packages / 76 source members. Its profile does not exist. Five #347 engineering packages remain necessary source/mapping dependencies unless explicitly excluded later. Optional maintenance and local backlog stay outside the default source-repository selection.
- #346's pending authorized scope stays the original nine-package / 68-member mapping. Later #356/#357 mapping is a separate actual-source continuation; this integration neither edits those blocked files nor replaces their owner.
- Retire each replaced ordinary route on both discovery surfaces only at its selected cutover. Codex is the only delivered new adapter; any affected Claude gap must be explicitly disposed before that cutover. Source governance/release/CI, old format readers, active recovery and retained project records remain separately owned.
- Select the proposed narrow byte-preserving attributes, project store/template bindings, separate project backups and staged root changes as inputs for the later adoption task. Fresh target collisions, effective attributes, actual roots, decision/effect evidence and public-reader results are still required. No root pointer, Git attribute, config, historical record or managed installation is changed now.

#361 observed a base without #359 implementation. Integration supplies [the actual writer handoff](../../../design/framework-next/managed-installation/handoff.md): ten engine files, `src/tools/maintain_framework.py`, `.ai/local/installation.guard`, `.ai/framework.operation` and the selected native/failure-domain limits. Its predecessor observations remain historical rather than being rewritten. Concrete pilot storage, activation and P7 checks are selected only after remaining source/mapping work is ready.

## Direct checks and retained preparation issues

Against the fixed Git commits, coordinator direct checks passed: **23 UTF-8 files, 4 JSON, 2 YAML, 6 AST parses, 61 local Markdown file targets**, clean worker Git state and changed-scope diff checks. JSON/YAML parsing is not schema validation; AST is not product import/execution. The five final #361 design hashes were compared with the preparation reads; the README's final text was reread after its final normalization, and all detailed proposal files matched.

One preparation read guessed #359's report name as `source-delivery.md`; it did not exist. The actual diff named `reports/remediation-report.md`, which was read. A first strict all-file raw-byte comparison stopped on that report: four #359 workflow documents had CRLF worktree materialization versus LF Git blobs. The corrected observation proved only CRLF-to-LF differences for those four documents; all six changed Python sources and both design files matched their Git blobs exactly. The fixed-commit syntax/reference checks then passed. No worker bytes were rewritten to hide that observation.

After combining the two deliveries, all 23 integrated Git blobs matched their worker commits exactly. Direct coordinator checks covered eight changed UTF-8 files, three JSON, one YAML and 44 local file targets (only changed index rows); staged/unstaged diff checks and the complete planned integration message passed.

Worker reports retain their earlier proxy, missing-path, command-length, inventory-extraction and whitespace failures. Their full planned commit-message checks passed before committing. These administrative/source checks are neither native backend evidence nor admission through suspended validators.

Product CLI/help/import, schema validation, tests/fixtures/build/package/install/migration/probes, independent audit/lease/effective-rule/acceptance packets and hosted CI remain **deferred-by-owner**, U001, owner program #322 coordinator / P7. #346/#347 direct authorization requests remain pending. No whole-program finding, root adoption, release or pipeline restoration is completed by this integration.
