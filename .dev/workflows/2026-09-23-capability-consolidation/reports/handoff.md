# Issue #342 local design handoff

Status: bounded design complete; retained in the containing local commit. Source implementation and adoption remain unselected; verification remains deferred-by-owner. This is an adapted U001 handoff, not a validated legacy audit/lease packet. Coordinator: 01a0c9d9-3b00-7b70-ad85-daff590e7ecd. Hidden context required: false.

## Identity and scope

Worktree F:/framework-next/342; branch codex/2026-09-23-capability-consolidation; baseline 94741016bd7baae9936bb77dd6c4f56d37036e97; persistent common Git dir C:/Github/YuChia/ai-collaboration-prompts-dotnet-backend/.git. Exact delivery HEAD is the containing commit of this handoff (resolve git log -1 --format=%H -- this file); callback supplies the full SHA after commit. No self-referential fabricated commit hash.

Only .dev/design/framework-next/capability-consolidation/ and .dev/workflows/2026-09-23-capability-consolidation/ changed. [Design entry](../../../design/framework-next/capability-consolidation/README.md) links every deliverable; [task](../tasks/P5-DESIGN-342.json) and [checks](../evidence/static-checks.json) retain actual outcomes.

## Coverage and recommendations

16 exact skills, 95 exact legacy kind rows (17 groups), five actual source packages/44 members/six record schema resources, and N01-N16 closed/generated format classes plus supplemental legacy diagnostic L96. Proposed kind dispositions: 33 source-only, 21 preserve-unsupported, 16 replaced-preserve, 14 retired-preserve, 6 optional-deferred, 4 portable-selectable, 1 external-preserve. Every original row/model/producer/validator is retained separately from proposed responsibilities.

Propose metadata-v3 instruction/tool union and explicit null configuration for no-owned-record packages; preserve v1/v2. First reasoning example code-reviewer has three expected members; actual ADR remains metadata-v2 with eight. Ordinary users need no context maintenance; source release/history excluded. M01 is only a future exact JSON config integer 1 -> 2 edge, owned by P6 for selected projects; no record migration.

D342-01..06 remain coordinator decisions. P4 #341 already has separately selected record 1.0.0/metadata-config v2, ten members/operations; source not yet returned at dependency observation. P3 mapping now integrated at ac4175948045590d1a1942022435ab438ad30ac3; earlier missing-mapping text is explicitly baseline history. No package source drift between baseline and P3 integration. See dependency-update.md.

## Limits and next action

Design only. No implementation, capability activation, installed state, runtime validation, compatibility, migration, CI or performance evidence. Tests/product tools/audits remain deferred-by-owner under U001 to program #322 coordinator / P7. Related #316/#320/#149/#168/#274/#275 scope read only; no state changes.

Coordinator reviews design checkpoint and selects future assignments/shared owner; first push, PR/merge and provider state belong to coordinator. Proposed shared index row (not applied):

| 2026-09-23-capability-consolidation | ai-context-governance | completed | #342 P5 design: 16 skills, 95 kinds, metadata-v3 proposal; implementation selection and P7 checks pending | .dev/workflows/2026-09-23-capability-consolidation/workflow-plan.md |

## Actual checks and retained failures

Direct row equality preserved all 95 original registry records, 17 nonoverlapping groups, 16 exact canonical skill paths, and five complete declared/tracked package member sets (44 total, six schema resources). ADR metadata specimen matches actual source bytes. Ten selected distribution/package Python texts parsed through ast.parse without importing products. Owned JSON/YAML/UTF-8 reads and 32 local Markdown file links resolved at the first static inspection; final changed data/links are reread before commit. These are source/inventory observations, not tests or schema compliance.

Initial GitHub read was blocked by the sandbox proxy, then succeeded with scoped read-only escalation. One initial registry query used a nonexistent kinds property, corrected to records before inventory generation. A combined write command exceeded Windows command-line length (os error 206) and never ran; split commands succeeded. Graph indexing reports no commit attestation and excluded legacy assets/scripts and skill scripts, so material claims use direct Git-tracked sources. These limitations remain in source-observation.json.

Commit uses the complete planned message from this workflow's ignored artifacts/commit-message.txt after its allowed message validator. Exact outcome, message digest, final staged scope and diff inspection are in static-checks.json. No push, PR, merge, Issue/Project mutation or source/root cutover occurred.

P6 design #345 now owns the installation/update/recovery proposal in its own task and will consume M01 after coordinator selection. Conversion implementation remains future work; #342 scope is unchanged.
