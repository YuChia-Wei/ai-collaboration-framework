# Five portable engineering methods

Workflow `2026-09-23-engineering-methods`; owning skill `ai-context-governance`;
[Issue #347](https://github.com/YuChia-Wei/ai-collaboration-framework/issues/347).
Created 2026-09-23T11:55:00+08:00. Adapted from the owning
`ai-context-maintenance-workflow-plan-template.md` version 1.2.0 under U001.

## Authority and source boundary

Use the [selected P5 contract](../../design/framework-next/p5-selected-contract.md)
and [U001 override](../../standards/FRAMEWORK-REDESIGN-EXECUTION-OVERRIDE.md).
The [direct owner confirmation](../2026-09-23-framework-redesign-control/reports/p5-owner-confirmed-resume.md)
resolves the original blocked write scope after two automatic approval refusals.
The same executor and branch continue; no substitute writer or sub-agent.
Model/effort are declared by dispatch as gpt-6-astra / ultra, not independently
attested runtime facts. Initial clean HEAD was
`842b73ca09d701d1561109255193d80439dc996b`; coordinator fast-forwarded the still
clean worktree to resumed HEAD `4cda6689bf469a2273e14e237a0c3bf7ad4b6eed`.

Exclusive source: five directories under `src/skills/` named
`diagnostic-analyst`, `ddd-ca-hex-architect`, `bdd-gwt-test-designer`,
`local-change-implementer`, `slice-implementer`; own
`engineering-methods` design and this workflow. Every repository command runs
in `F:/framework-next/347`; every edit uses its absolute path. Preserve other
writers, shared manifest/profiles/adapter/loader/indexes, root entries, legacy
source and `F:/ai-context-tests`. First push and online integration belong to
the coordinator; local commit does not mean Issue closure or root adoption.

## Outcome and completion criteria

Deliver five independent version 0.1.0 metadata-v3 instruction packages. Each
has null configuration, empty skill dependencies and no artifact roles, schemas,
templates or tools. Package-contained references retain actual specialist
methods and accept explicit target rules, commands and destinations. No global
technology baseline, diagnostic JSON 1.0 port or invented executable.

Preserve diagnostic falsification/uncertainty; architecture invariants,
alternatives and fixed-artifact review; observable GWT/test-level reasoning;
local semantic radius; and command/query/reactor/generic slice ownership.
Instruction/source presence never means execution or runtime acceptance.

One task, `ENG-347`, owns the coherent five-package delivery. This workflow is
needed to retain source extraction, interrupted permission history and the
cross-session coordinator integration/P7 boundary, not to pad task counts.

## Sequence and checks

1. Read the assigned sources and extract useful portable methods.
2. Author complete packages and a source-only exact member/operation handoff.
3. Direct UTF-8/YAML/JSON, contained references/members, content and Git diff
   checks; validate the complete planned commit message before local commit.
4. Return full HEAD, clean state, actual checks and deferrals in the task final.

Product CLI/help/import, schema validation, tests/fixtures/build/render/package/
install/migration/probes, legacy validators except the exact message check,
audit/lease/acceptance machinery and CI are `deferred-by-owner`: authority U001,
owner program #322 coordinator / P7, next action P7 selects redesigned checks
after implementation. No synthetic passing evidence or independent audit record.

## Resume

Task: [ENG-347](tasks/ENG-347.json). Source rationale and exact inventory belong
in [the design](../../design/framework-next/engineering-methods/README.md).
Actual/deferred checks and retained failures belong in
[the report](reports/remediation-report.md). Completion of this bounded source
work is separate from shared mapping, integration, runtime use and P7 acceptance.

Current local state: source task completed at 2026-09-23T12:03:24+08:00. Direct checks covered 29 UTF-8 files, six YAML documents, two JSON documents, five skill frontmatter documents, 33 local links and 23 exact members with matching inventory hashes. The complete planned message check passed; all source and this workflow form one local commit whose final HEAD is returned through this task. No product acceptance is claimed.
