# P7-B source pipeline design

- Workflow: `2026-09-23-pipeline-redesign`; task: `P7B-DESIGN`;
  [Issue #365](https://github.com/YuChia-Wei/ai-collaboration-framework/issues/365).
- Owner skill: ai-context-governance; proportional template source:
  `.ai/assets/skills/ai-context-governance/templates/ai-context-maintenance-workflow-plan-template.md`.
- Execution authority: coordinator's explicit P7 design assignment and live Issue;
  [U001](../../standards/FRAMEWORK-REDESIGN-EXECUTION-OVERRIDE.md).
- Dedicated worktree `F:/framework-next/365`, branch
  `codex/2026-09-23-pipeline-redesign`, source
  `171f33474f88888fbe853600de04bfe9c5716b25`; persistent common Git
  `C:/Github/YuChia/ai-collaboration-prompts-dotnet-backend/.git`.
- Declared independent GPT-6 Astra / ultra assignment; no sub-agents or other tasks.

## Scope and acceptance

Produce [one concrete design](../../design/framework-next/pipeline-redesign/README.md)
covering all seven IDs, current source/hosted gate mismatch, exact future files/
contexts/settings, mandatory-gate dispositions, small source rules and staged
#361 adoption. Record fresh read-only provider evidence, actual direct checks
and material decisions for coordinator reconciliation with #364.

Only the design subtree and this workflow are writable. No root, source, tests,
index, standards, .github, provider or credential modifications. No push/PR/merge.
One substantive task is appropriate because the unique durable state is the
seven-pipeline/source-policy transition and its owner adoption boundary. There
are no padded audit or lifecycle tasks.

## Proportional U001 treatment

The workflow/task/report preserve IDs, explicit offsets, owner, input identity,
scope, actual findings and next actions. Native handoff schema, critical gates,
independent review packets/leases, effective-rule and acceptance-ledger machinery
are deferred under U001; this is not legacy schema compliance or a formal audit.
The task JSON's resume block and [handoff report](reports/design-handoff.md)
supply the receiving checkpoint without a self-referential commit record.

All product CLI/help/import, schema validators, tests/fixtures/build/render/package/
install/migration/probe and CI are **deferred-by-owner**, U001,
**program #322 coordinator / P7**. Next action: reconcile #364 and select implementation/
trials, then actual root adoption and exact user-approved restoration separately.

## Execution and result

Read source inputs and fresh provider settings; prepare proposal; directly check
UTF-8, JSON/YAML syntax, changed local references, scope and Git diff; validate
complete ignored planned message with the allowed commit-message validator; commit
one coherent delivery and read clean status. The final full SHA is reported from Git.
Provider integration, design adoption, product verification and Issue/Project
closure remain coordinator-owned and separate from design completion.