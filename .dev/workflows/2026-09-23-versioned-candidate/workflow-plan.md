# Versioned candidate execution

Workflow `2026-09-23-versioned-candidate`, task `VC01`, owner `slice-implementer`, generic mode. Created/updated 2026-09-23T22:34:24+08:00; in progress. Adapted from `.ai/assets/skills/software-development-orchestrator/templates/development-workflow-plan-template.md` v1.4.0 under U001.

Authority: [Issue #381](https://github.com/YuChia-Wei/ai-collaboration-framework/issues/381), [U001](../../standards/FRAMEWORK-REDESIGN-EXECUTION-OVERRIDE.md), the committed coordinator assignment, and the user's direct confirmation in this task of exact F: source/workflow/design/test/evidence/local-commit scope. Baseline `031233202f21e0793c9667cac872375d65e52be0` was clean at `F:/framework-next/381`, branch `codex/2026-09-23-versioned-candidate`.

One task covers the coupled candidate/reader/CLI behavior, a clean-source execution checkpoint and a later evidence commit. Own only Issue-listed product/test files, this workflow, and the [design contract](../../design/framework-next/versioned-candidate/contract.md). No component payload, bootstrap closure, apply/recover, target or shared-record edits.

## Selected execution

1. Strict explicit distribution versions, schema-2 versioned identity, legacy development preserved, complete explicit 18-component Codex profile.
2. `python -I -B tests/framework_next/test_versioned_candidates.py --mode regressions --output-root F:/framework-next/p7-runs/versioned-candidate`: tiny labelled fixtures, shared reader refusal/lock/planning tests. Retain failures and only retry after repair.
3. Readability/AST/JSON/YAML/link/scope/diff checks; exact planned-message validation; coherent implementation commit.
4. From that clean commit, run the same test entry with `--mode complete`: one real public CLI rc.1 assembly and installation reader, exact 18 identities / 113 payload / 18 runtime comparison. Cap 512 files and 16 MiB; retain residue.
5. Separately `--mode fixture-plan`: manual rc lock and fictional future stable metadata through the existing public read-only planner, exact expected lock hash and immutable engine pin. No actual release/upgrade/apply/recover.
6. Commit truthful evidence; return clean local HEAD before first push. Coordinator owns integration and pilot decisions.

## Boundaries and retained preparation evidence

U001 requires this independent Astra/ultra task and prohibits subagents/nested tasks. Effective-rule/formal/native handoff packets, independent audit, legacy/full/history/profile matrices and hosted/CI gates stay `deferred-by-owner`; owner program #322 coordinator / P7, next action explicit P7 check selection and adoption. No push/PR, provider/Issue/Project writes, release/tag/publication, root adoption, credentials or downstream writes.

The graph was freshly indexed as `framework-next-381` at baseline; installation-state discovery was confirmed against source. The fast graph excludes tools, so the named CLI was read directly. Preliminary missing graph project argument/guessed file lookups made no mutations and are not test results. Automatic review twice refused workflow/design/evidence-directory creation: forwarded/read-back owner approval was insufficient trusted authorization. No rejected command executed. The user then directly confirmed this exact scope in this task; these are resolved historical refusals, not pending approvals. Inspection corrected two test-draft field references before the first test run.

## Implementation checkpoint (2026-09-23T22:35:42+08:00)

Six focused tests passed in 0.304 seconds (run wall 0.370 seconds), with 34 retained files / 39,430 bytes. [Transcript](evidence/regressions-01.txt). This is tiny synthetic source/lock/future-stable evidence, not a release or target upgrade. Static parsing, exact declared component versions/member count, unchanged prior profiles/components, changed-link/scope checks and git diff --check passed. AST comparison confirmed only the intended existing assembly/selection/identity functions changed; bootstrap, loader, native IO, planner, apply/recover and payload bytes remain unchanged. The actual complete CLI and public pinned-engine fixture plan remain pending until the clean implementation commit.
