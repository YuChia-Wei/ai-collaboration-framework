# Approved bootstrap path continuation

Resumed from clean 9c27dd419116245426995580247ecdf7bf6358c8 on the same assigned
F:/framework-next/378 worktree and codex/2026-09-23-windows-path-compatibility.
Live #378 was read back with Approved continuation: bootstrap path predicate,
OPEN, no comments. The first scoped write request was rejected by automatic
approval review because forwarded transcript authorization did not establish
this security-sensitive source scope. Read-back confirmed zero changes and the
same clean HEAD. The owner then directly confirmed in this task:
"確認此範圍並繼續" for the exact source/helpers/tests/workflow, local commit and
one full public plan. The refusal is historical evidence, not pending approval.

The refreshed graph excluded src/tools and returned no _direct symbol; use the
tracked file fallback for this explicit owner. local-change-implementer remains
the owning route, framework-source, with U001 proportional records and suspended
legacy packets/CI. No sub-agents, executor-created tasks or callback.

The only additional product owner is maintain_framework.py::_direct plus private
stdlib-only helpers for canonical Windows path and stable direct identity.
WinError 1 alone may use direct DOS mapping and native long-name checks; file
versus directory kind, hardlink count, path/file limits, all ancestor links and
reparse points, nonzero identities and stable before/after observations remain
required. The complete pin/ENGINE_FILES and verified retained-byte loader,
isolated/no-bytecode host, operations and recovery are unchanged. No distribution
import is used for pre-pin path validation.

Pending: focused bootstrap/path regressions, clean exact engine commit, and one
fresh actual full public plan. No success is inferred from this source edit.

## Focused source checks

`python -I -B tests/framework_next/test_windows_paths.py --mode bootstrap-regressions`
first run: 7 tests passed, zero skips, 0.064s. These are explicitly simulated
Windows API/path responses, covering ordinary and error-1 file/directory admission,
other OS/error refusal, lexical/short/DOS aliases, missing native name/mapping,
ancestor reparse/link/kind/nonzero identities, file hardlinks/size/path budgets,
identity and protection drift, and pre-pin root/path refusal before loader/import.

The identity helper was then kept in original ancestor-first traversal order;
a focused rerun is selected before the source checkpoint. No loader changes.
AST comparison with 9c27dd419116245426995580247ecdf7bf6358c8 proved main,
_VerifiedSourceFinder, ENGINE_FILES, pin/host checks and all other preexisting code
unchanged, apart from _direct and the required stdlib os import. Both new helpers
are private; all new native calls are inside the observed Windows error-1 arm.

Final focused path rerun: 7 passed, zero skips, 0.062s. The new public-plan
selection uses the existing tiny fixture helpers and asserts planned/exit 0,
exact engine/candidate binding, canonical plan SHA-256, satisfied native backend
and writer engine closure, plus untouched project/scratch/recovery roots. It is
one reader/plan selection, not a native runner or apply/recover matrix.

Actual public plan is still pending at this source checkpoint. All prior failure
runs and first-phase positive observations remain retained.
