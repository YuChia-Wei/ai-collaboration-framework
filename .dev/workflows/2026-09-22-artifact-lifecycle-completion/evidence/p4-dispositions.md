# P4 check and test dispositions

| Surface | Disposition | Evidence and limitation |
| --- | --- | --- |
| Workflow/assessment checks inside disjoint role/catalog/target/skill/lifecycle preview | Remove duplicate calls | Each operation has a fixed output domain; owning candidate validator remains. A negative fixture preserves workflow rejection while allowing an unrelated catalog preview. Full repository admission is unchanged. |
| Workflow/assessment linked bundle validation | Retain | Locator, index, lifecycle, identity and archived report relationships exceed serializer shape. |
| Canonical/schema/owner validators | Retain public CLI and gate IDs | Shared candidate helpers prevent rule drift; independent hand-authored inputs remain necessary to catch shared implementation defects. |
| Provider availability and invocation checks | Retain | Metadata edits cannot establish current runtime or actual invocation. |
| Hash/authority freshness, observed directory membership, committed source partition | Retain and extend | Apply re-derives preview; source disposition binds committed tree, shell binds index modes, skill binds runtime projection, lifecycle binds model and owner bytes. |
| Unknown fields, comments, duplicate keys, exact types | Retain owner-specific behavior | Open extension preservation and closed schema refusal are different intentional contracts. |
| Transaction journal/rollback and hostile paths | Retain | Serialization cannot prove recovery or custody. Shared engine tests plus new multi-file generated wrapper recovery cover distinct behavior. |
| Evaluation expected fixtures and actual result producers | Retain separately | Static corpus edit does not execute cases. Expectations remain independently supplied, not inferred from writer output. |
| Portable dependency/package smoke | Retain | Source imports can mask missing packaged helpers; isolated CLI and archive membership are separate checks. |
| Existing test methods | Remove none | No additional redundant behavior removal has equivalence evidence in this scope. New tests target new operation boundaries; earlier P1/P2 and Issue312 reductions retain their own historical evidence. |
| Hosted required contexts | Retain definitions; not executed here | Owner authorized local integration for this stage. This exception is not a passing hosted result or a policy change. |

No timing or token improvement is inferred from call-count reduction. Focused timings are observations of different selected commands, not a controlled benchmark.
