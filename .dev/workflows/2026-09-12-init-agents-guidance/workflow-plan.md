# Task-Led Initialization AGENTS Guidance

## Workflow Metadata

- `workflow_id`: `2026-09-12-init-agents-guidance`
- `workflow_kind`: `ai-context-maintenance`
- `owner_skill`: `ai-context-governance`
- `branch`: `codex/2026-09-12-init-agents-guidance`
- `base_branch`: `main`
- `status`: `completed`
- `current_phase`: `completed`
- `created_at`: `2026-09-12T13:17:49+08:00`
- `updated_at`: `2026-09-12T13:42:33+08:00`
- `template_source`: `.ai/assets/skills/ai-context-governance/templates/ai-context-maintenance-workflow-plan-template.md`
- `template_version`: `1.2.0`

## Objective And Authorization

Issue [296](https://github.com/YuChia-Wei/ai-collaboration-framework/issues/296) records the user's instruction to implement the accepted init AGENTS analysis. This delivery is independent from Issue 213 and starts at clean main `7d3274e53fdc13fbbb322c493407efae70bb930a`. Local implementation, validation, independent review and commit were the original authorized stage. On 2026-09-12 the owner additionally authorized public push, PR creation, checked merge, Issue 296 closure and Project read-back. Tag creation and publication remain outside this delivery.

## Accepted Scope And Criteria

1. Load context progressively from the task and owning skill; avoid whole-document catalogs.
2. Make Repository-Specific Context the primary init replacement zone, with only narrow evidence-backed navigation adjustments elsewhere.
3. Preserve authorization across the same scope and action; resolve ordinary reversible work without repeated approval, retaining owner-sensitive boundaries.
4. Use target-owned commands and narrow required validation; repeat or broaden only when evidence warrants it.
5. Keep one conditional CLI-contract link in the seed and preserve consent semantics with their canonical owner and immediate regression test.
6. Keep English canonical for consistency and maintenance, without universal model or token-efficiency claims.
7. Preserve target-owned facts, English/zh-TW parity rules, thin CLAUDE and package/schema compatibility; complete independent verification.

## Evidence And Artifacts

The baseline was transient read-only analysis, not a retained assessment instance. Its unchanged receipts remain under ignored `.tmp/ir/.dev/ai-context/local/init-agents-review/` and `init-change-checks-2/`. The stable finding IDs in task INIT296-001 map to that analysis and compatibility advice. Do not recast those reports as remediation or verification.

- Task: `tasks/INIT296-001.json`
- Remediation report: `reports/remediation-report.md`
- Local command evidence and bounded independent audit: ignored `.dev/ai-context/local/2026-09-12-init-agents-guidance/`

## Current Resume State

All planned implementation and independent source verification are complete. No additional source edits are planned. Root owns final record validation and acceptance; their latest actual outcomes belong to the ignored `final-checks/` and `final-audit-2/` receipts. Those read-only gates do not authorize public delivery.

## Historical Implementation Checkpoint

At the implementation checkpoint, all nine CLI routing tests, AI-context validation, workflow validation and whitespace checks passed. Independent fixed-head verification was then the next action; it subsequently passed on `9d9dc1b5bfc0cbbceb0780946032231b8cadd896`. This paragraph records history, not an outstanding action.

## Local Completion

Independent verification of `9d9dc1b5bfc0cbbceb0780946032231b8cadd896` passed with no actionable findings. All seven accepted criteria are resolved in INIT296-001 and the remediation report. The reviewed four source files match the actual tested Git blobs. The earlier task-state checkpoint failed workflow validation; its non-passing evidence and pre-dispatch cancellation remain retained, and the corrected state passed before actual review. Final record validation and immutable-subject reconciliation complete this local delivery. At that local checkpoint no provider delivery had been performed; the later authorization below governs PR 297.

## Final Record Repair

The first metadata audit failed on stale current-action wording and missing final-subject workflow receipts. Its failed terminal is preserved. INIT296-FINAL-001 is repaired by the current/historical separation above. INIT296-FINAL-002 requires fresh workflow and whitespace receipts on the repaired clean commit, followed by one second metadata audit. The actual terminal outcomes remain in the declared ignored evidence roots; no passing outcome is presumed. The four source files and their accepted semantic review remain unchanged.

## Authorized Provider Delivery

PR 297 is the terminal delivery for Issue 296. The source guidance and seven-criterion acceptance are unchanged from the passed c11c01ca audit. Current-content declaration review, required hosted checks and live admission precede the authorized merge; actual integration and Issue/Project outcomes remain in ignored provider receipts. Earlier local-only statements are historical scope, not current authorization. The workflow stays completed because all workflow-owned source work and verification are complete. Release readiness and theme synthesis are separate read-only work.
