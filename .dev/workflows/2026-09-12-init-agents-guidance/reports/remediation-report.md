# Initialization AGENTS Remediation

## Changed Behavior

The public seed now loads context by task and phase, keeps ordinary reversible work within existing authorization, and separates local work from hosted or publishing actions. Initialization has one primary target-fact replacement zone with narrow evidence-backed navigation edits elsewhere. Target commands include working directories and are distinguished from proven successful execution.

CLI consent details remain with the unchanged canonical contract. The existing regression test follows the seed's contract link and checks the owner's path, consent, action disclosure, secret exclusion and decline behavior. English remains canonical for consistency and maintenance; the language rationale makes no universal token or model-quality guarantee.

## Finding Reconciliation

| Baseline finding | Implemented response | State |
| --- | --- | --- |
| INIT-AGENTS-001 | Task-led progressive loading replaces unconditional document preloading. | resolved |
| INIT-AGENTS-002 / INIT-COMPAT-002 | Primary replacement zone and narrow outside edits preserve target truth and portable baseline. | resolved |
| INIT-AGENTS-003 | Existing authorization persists; new authority and actual delivery states remain explicit. | resolved |
| INIT-COMPAT-001 / INIT-COMPAT-003 | Seed links the existing CLI owner; its direct test verifies preserved canonical consent obligations. | resolved |
| INIT-COMPAT-004 | English and translation ownership remain; consistency replaces unsupported universal token rationale. | resolved |

## Validation And Limits

All nine CLI routing tests, AI-context validation, workflow validation and whitespace checks passed. Actual command receipts and output hashes are retained under ignored `.dev/ai-context/local/2026-09-12-init-agents-guidance/checks/` and summarized in INIT296-001.

The package seed mapping, thin CLAUDE adapter and CLI schema are unchanged. This is a source/template change: no downstream initialization, model benchmark, public push, PR, merge, Issue closure, tag or release is claimed. Independent source verification passed on `9d9dc1b5bfc0cbbceb0780946032231b8cadd896` with no actionable findings. The accepted report SHA-256 is `442da0f98df68dbc007e07445ee6b8d2239c0b5902aa3b2f2e90c6e2ad510371`. The read-only terminal, parent verification, actual test receipts and seven-criterion acceptance ledger/projection remain under the declared ignored evidence root. Final metadata verification is separate from this unchanged source review.

The workflow task state was corrected after a failed checkpoint gate, before any agent invocation. That failure remains recorded and does not represent a passing check; the corrected workflow passed. All baseline findings now have explicit resolved outcomes. Local implementation and review are complete; Issue 296 remains open for separately authorized provider delivery.
