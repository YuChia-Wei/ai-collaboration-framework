# Portable Review And Design Skill Capabilities

## Workflow Metadata

- workflow_id: `2026-09-10-review-skill-capabilities`
- owner_skill: `ai-context-governance`
- branch: `codex/2026-09-10-review-skill-capabilities`; base_branch: `main`
- status: `completed`; current_phase: `completed`
- created_at: `2026-09-10T09:32:33+08:00`; updated_at: `2026-09-10T10:19:31+08:00`
- template_source: `.ai/assets/skills/ai-context-governance/templates/ai-context-maintenance-workflow-plan-template.md`
- template_version: `1.2.0`
- Source baseline: `a6110a34d17d15a200aa2f7ebca0415ad8a37c22`; clean local main matched the live remote main before branch creation.

## Objective And Authorization

Implement [#289](https://github.com/YuChia-Wei/ai-collaboration-framework/issues/289) and
[#290](https://github.com/YuChia-Wei/ai-collaboration-framework/issues/290).
The owner authorized local modifications and sub-agent scheduling on 2026-09-10,
selected Terra xhigh or Luna max for child work, assigned final verification to
the parent, and requested completion callbacks instead of polling. Prior Issue
planning-only restrictions are superseded for this local implementation scope.
No transport, merge, provider terminal state, release or publication is authorized.

Keep `code-reviewer` as the common entry and retain the selected .NET extension.
Keep architecture and BDD skill identities and evaluate design/review modes
within them. No new skill, broad role infrastructure, framework installation,
company scan or new technology profile is part of this delivery.

## Delivery And Execution Boundaries

One workflow preserves shared package/routing integration, validation and
independent review state. Each Issue retains its own acceptance ledger.
The two changes share a release horizon, context ownership, runtime contracts
and regression surface; one local integration unit is appropriate.
Future PR and merge topology remain separate decisions.

The parent holds the sole tracked-writer lease. The current governance skill
has no canonical writer-role binding; do not relabel another skill's role to
bypass the guardrail. Initial integration is parent-inline. Once a clean
remediated commit exists, dispatch bounded read-only Terra xhigh reviews through
the applicable `ai-context-auditor` fixed-head role in isolated checkouts.
Use one terminal callback per child and inspect returned evidence after completion.
Root integration and acceptance are not delegated. Preserve corrections and
failed attempts; retry only after a material change.

## Baseline Findings And Design

1. #289: the reviewer skill, wrappers and all review roles are owned by the optional
   .NET component. Its routing contract starts from C# and cannot serve core-only
   installations. Move common and method-specific review reasoning and all four role entries into
   software development core; retain the fourteen .NET routes and concrete
   technology standards under the .NET profile.
2. #290: BDD names BDDfy as an unconditional default; architecture references bind
   concrete .NET/MQ conventions. Add explicit artifact review contracts, preserve
   method ownership, and select concrete tooling through target decisions.
3. Existing role/effective-rule and package contracts are part of compatibility.
   Generic review must not claim absent specialist coverage or bypass required
   target-effective rules. Actual skill exercises are required by both Issues.

## Tasks And Acceptance

| Task | Scope | Status |
| --- | --- | --- |
| R01 | #289 common core, .NET extension, ownership and routing | completed |
| R02 | #290 design quality, review modes and handoffs | completed |
| V01 | Focused deterministic checks and actual skill exercises | completed |
| A01 | Independent review, parent reconciliation and report | completed |

Record every acceptance checkbox from each live Issue independently. A fixture
or static keyword assertion supports a contract test but cannot establish that
an agent executed a skill correctly. Controlled examples may supply actual
runtime inputs; label them as controlled examples and retain the observed output.

## Validation Plan

Start with YAML/JSON/reference validation and focused affected tests. Verify
core-only and .NET-selected package ownership and navigation on a committed
subject; preserve .NET route identities. Exercise generic, .NET, mixed and missing
technology review plus architecture/BDD review and design handoffs. Include
known defects, acceptable alternatives, incomplete input and read-only checks.
Choose wider package/migration checks only from actual impact. Long-running
commands use one external read-only task and a terminal callback on a fixed
clean commit; no release/nightly/full history matrix is selected by default.

## Evidence And Resume

- Runtime guardrails: `.dev/ai-context/local/review-skill-capabilities/` (ignored).
- Graph reindex was attempted; it excludes `.ai/assets` and `.ai/scripts` and
  does not report a commit SHA. Explicit tracked-file discovery is used for this
  scope; graph absence is not evidence.
- All local tasks completed; source subject `deb969de2a8e71f7b6ef218c35d0b060d9b49142`.
- See [delivery report](remediation-report.md), [acceptance index](acceptance-index.json) and [observed results](observed-results.json) for separate Issue acceptance, original failures and verification limits.
- Final clean-commit admission writes only ignored artifacts under `.dev/ai-context/local/review-skill-capabilities/final_admission/`; its future outcome is not asserted by this tracked closeout.
- Local branch only; no push, PR, Issue/Project terminal change or release.
