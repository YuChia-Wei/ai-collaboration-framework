# Source gate implementation checkpoint

Owning skill: ai-context-governance, applied proportionately under U001.
Online scope: https://github.com/YuChia-Wei/ai-collaboration-framework/issues/369.
Selected design: [P7 selection](../../design/framework-next/p7-execution-selection.md).

Current checkpoint: V1/V2 interface provenance and finite selector ownership bind
integrated source `e71712b71791170c3f4946e131ce867f82dade8f`. Read the
[current handoff](reports/integrated-binding-handoff.md). The original clean
`0725c3dd68e6d1cb4aca5d03343cd239b8c447a1` was fast-forwarded only, in the same
F:/framework-next/369 worktree and codex/2026-09-23-source-gates branch.
Tracked coordinator evidence binds this task UUID to #369, separately from #368.

Only the selector, its existing small tests and this Issue's own records change.
The actual source harness passed 44/44, zero skips, in 1.959 s (capture 2.203 s).
Raw output, tested working-tree hashes and unchanged fixture-parent observations
are retained in [attempt 1](evidence/integrated-binding-attempt-1.json).
New result inputs are synthetic. No product/public/native/hosted test ran here.
Caps, result parser semantics, process execution and command dispatch are unchanged.

## Retained checkpoints

- [Initial refusal/failure](reports/blocked-handoff.md): broad policy write refusal
  and original WinError 1; separately authorized fixture correction then passed 24.
- [Contracts binding](reports/runner-binding-handoff.md): original #368 interface;
  28 source tests, synthetic command responses.
- [Public binding](reports/public-binding-handoff.md): original #373 interface;
  37 source tests, synthetic full-family responses.
- [Dormant policies](reports/dormant-policy-handoff.md): direct owner confirmation,
  16 additive policy/root/template files and unchanged effective YAML values.

These reports retain their original sources and failures. The current continuation
does not rewrite historical results or declare Issue #369 complete.

## Remaining owners

Known native/regression paths now identify their owners and separate admission
requirements without dispatching new tests. Unknown paths remain fail-closed.
Native caller/termination/hosted portability is outside this continuation, as are
all tests/framework_next edits, product trials, policy activation and provider work.
#368/#373 own their current selected checks. The coordinator reports target pilot
review/admission passed and lease released; this task did not re-execute or verify
that target and does not infer source-policy adoption from it.

Coordinator owns independent source review, integration/first push/PR/merge,
new native authority and the exact adoption/restoration decision. CI and unselected
legacy schema/packet/matrix verification remain deferred-by-owner under U001.
