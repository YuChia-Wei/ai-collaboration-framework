# Issue #369 dormant policy handoff

Status: **original dormant policy scope implemented; not adopted or effective**.
This report supersedes the earlier policy-write approval block only. Source gates
remain a partial local delivery with unresolved actual product/native acceptance;
Issue #369 is not declared complete and no provider state was changed.

Worktree: `F:/framework-next/369`.
Branch: `codex/2026-09-23-source-gates`.
Parent: `289852bd6661be31acfcfb16bef3f2ebd50629f2`.
Record identity: containing commit; exact full SHA returned with final handoff.
Owner: program #322 coordinator / P7, under U001.

## Direct authority and previous refusal

The user directly confirmed the original policy, AGENTS and PR template scope:

> 確認 #369 原定政策、AGENTS 與 PR template 寫入範圍；僅限尚未生效的規範及適用性指標，不啟用規範或 CI。

This is approval to write the already proposed dormant rules and applicability
pointers. It is not adoption of those rules, CI restoration, root-route adoption,
release authority, first-push authority or permission to mutate provider state.
The scoped write was submitted through normal approval and succeeded.

The [original rejection](blocked-handoff.md) is retained unchanged, including the
reason that trusted policy-write authorization was then absent. No alternate tool,
writer or destination bypassed it. The direct confirmation is the changed authority
for this write; the earlier fixture-only permission supplied no policy authority.

## Delivered scope

- New [canonical prospective policy](../../../standards/SOURCE-DEVELOPMENT-POLICY.md):
  eight dormant rules, specific future gate dispositions and separate activation
  conditions from #365. Existing U001/effective policies remain authoritative.
- Matching dormant pointers in English and Traditional Chinese root AGENTS.
- Concise applicability paragraphs in the ten originally selected Markdown policy
  surfaces, including Git flow, workflow, terminal, context, work authority,
  commits, U001 and release; existing bodies remain intact.
- Comment-only pointers in both effective YAML files. All values and exact data
  types are retained; no dormant key, hybrid receipt or effective context change.
- Dormant HTML preview comment in the lower-case `.github/pull_request_template.md`;
  the complete existing active form and checklist are retained.
- Only this Issue's design/workflow/task/current handoff records accompany them.

The original file list remains in the rejection report and the current task's
scope. No source runner, test, workflow, commit-policy YAML, installed route,
configuration, data, shared coordinator record or legacy support code was edited.

## Direct verification

Read-only Python comparisons against the full parent above verified all 16 policy
files against the approved path set. Removing only the inserted paragraphs,
sections or comments reproduces all 15 existing bodies. YAML comparisons use
recursive exact types, not coercive equality. Results: **passed**, 15 preserved
bodies, 2 identical parsed YAML documents, 15 added local references resolved,
UTF-8/final newline/trailing whitespace/conflict-marker checks passed.

Author inspection confirmed the same meaning in both AGENTS additions: dormant,
source-only after adoption, no effective-section/route/U001 replacement, separate
recorded policy adoption and CI restoration. This is not independent review.
`git diff --check` passed; diff for source scripts/tests/all workflows and executable
commit policy is empty. Existing policy edits are additive only.

The exact planned message passed
`python -I -B .ai/scripts/validate-git-commits.py --message-file F:/framework-next/369/.dev/ai-context/local/commit-messages/issue-369-dormant-policy.txt --workflow-id 2026-09-23-source-gates-implementation`.
This is message validation only; no history or aggregate profile was invoked.

The earlier 24/28/37 source-test results are retained in their original reports;
no runtime code changed and those tests were not rerun for this document change.
No actual public/native/product/hosted suite or legacy validator matrix ran.
The original WinError 1, upstream #368 errors and incomplete #373 results remain
failures/limits; local policy checks do not change their outcomes.

## Next action and boundaries

Coordinator receives this local commit with the prior source gate/binding commits.
Before any later activation, reconcile the effective policy/root/template/runner
cutover, upstream acceptance fixes, actual selected trials, native binding and
independent scoped review. Present the exact adoption scope/date, U001 remainder
and settings/workflow delta for a separate owner decision. Adoption and actual
hosted success are separate observations; no missing check is treated as passed.

V1 contracts interface remains bound to `070a47335ffce99d31bd83e487447942539e4a9f`;
V2 public interface to `7996b32d3d4f70553b203299e25dc69d9413ff9d`. Their source success
fixtures remain synthetic; #368 metadata/filesystem failures and #373 incomplete
families remain unresolved here. V3 still has no actual native binding and exits
blocked. See the [public handoff](public-binding-handoff.md) for these limitations.

No new provider read or mutation was performed in this follow-up. The prior
observation of disabled Actions/workflows remains a historical read, not refreshed
hosted evidence. No push, PR, merge, Issue/Project mutation, credential change,
installation, release, publication or support withdrawal occurred. CI and
unselected legacy verification remain `deferred-by-owner`, coordinator / P7.
