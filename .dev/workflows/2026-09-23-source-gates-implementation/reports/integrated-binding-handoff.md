# Issue #369 integrated interface and ownership handoff

Status: **bounded original-scope continuation complete locally; no policy/CI adoption**.
Worktree: `F:/framework-next/369`.
Branch: `codex/2026-09-23-source-gates`.
Prior clean HEAD: `0725c3dd68e6d1cb4aca5d03343cd239b8c447a1`.
Exact fast-forward base: `e71712b71791170c3f4946e131ce867f82dade8f`.
Local delivery identity: containing commit; full SHA returned in final handoff.

## Scope and identity

The coordinator continued the original #369 selector/test/own-record scope. Its
reported prior dispatch rejection had misidentified this task as #368. This task
independently read the fixed tracked coordinator-transfer record: UUID
`01a0cd11-7b98-7090-ae53-1bf7bf46a651` maps to #369/F369; #368 maps to the distinct
`01a0cd11-1dc9-7842-abba-65b8fac0edb8`/F368. The claimed prior rejection reason is
coordinator-reported; it was not independently retrieved here. Normal scoped
approval then permitted the exact fast-forward, code changes and focused tests.
No executor, task, worktree, authority boundary or alternate writer was substituted.

Only `.github/scripts/check-source-change.py`, its existing
`.github/tests/test_source_gates.py`, and this Issue's own records changed.
No product or tests/framework_next file, native caller, workflow, policy/root/
template, shared coordinator record, credential or provider setting changed.
There was no agent/task creation, fetch, push, PR, merge-to-main or publication.

## Actual interface and ownership changes

Both interface provenance constants now identify the integrated base above.
V1 remains `python -I -B tests/framework_next/run.py --layer contracts` with its
unittest/runtime/cleanup observations. V2 remains that runner's
`--layer public --family ID`, with separate three-row JSONL/stdout and unittest/
stderr. The seven phase lists still match the inspected integrated source.
These are interface identities, not a claim that product checks ran at this base.

The integrated runner retains pre-cleanup accounting on cleanup failure. New
synthetic V1/V2 cases reject that result despite successful assertions, full
phase lists or a forged zero process exit/final row. An additional successful
`measurement_phase: before-cleanup` observation remains accepted. Actual result
parser functions are unchanged; no duplicate product protocol was introduced.

| Exact path under tests/framework_next/ unless stated otherwise | Owner/selection |
| --- | --- |
| test_knowledge.py | All seven public families because PublicCase is shared with work/CBF. |
| test_work.py | PR, local-backlog, software-development-orchestrator. |
| test_cbf.py | Problem-frame-author. |
| tools/build-candidate.py (repository-relative) | Distribution: existing contracts/declared consumers plus separate versioned-candidate trial requirement. |
| test_native_windows.py | #382 native driver; V3 caller still pending, independent scoped review required; no native dispatch. |
| test_engine_source.py | #371 engine-source regression owner. |
| test_installation_scan_budget.py | #386 scan-budget regression owner. |
| test_pr_git_worktree.py | #335 PR-worktree regression owner. |
| test_protected_paths.py | #383 protected-path regression owner. |
| test_versioned_candidates.py | #381 versioned-candidate regression owner. |
| test_windows_paths.py | #378 Windows-path regression owner. |

The six named regression paths emit `owner-selected-regression-required:<path>`
and only content/whitespace checks. They do not gain executable commands or pass
results. Their admission requirements remain for the owner; a source-only pass
cannot satisfy them. Unknown filenames still fail, and deletion/rename retain
both sides' ownership and requirements. No recursive test discovery was added.

## Actual focused verification

Command:

```text
python -I -B F:/framework-next/369/.github/tests/test_source_gates.py --output-root F:/framework-next/p7-runs/369-source-gates
```

One execution: **44 passed, zero failures/errors/skips, exit 0**; harness 1.959 s,
capture 2.203 s. [Raw output and tested hashes](../evidence/integrated-binding-attempt-1.json)
retain the exact interpreter/argv, base and tested working-tree bytes. Those bytes
were unchanged after execution and are bound to the delivery commit by read-back.
No prospective immutable-commit execution is claimed. The explicit fixture parent
was empty before and after; the existing unique tiny Git child was cleaned.

All product/public result inputs in this harness are synthetic. Existing probes
launch only bounded Python capture examples and the tiny two-commit Git fixture.
No #368/#373 suite, new regression, real native case or hosted run was launched.

Direct AST comparison with the base confirms the only changed existing selector
function is `classify`, plus the two interface constants and two finite maps.
Caps, command_for, run_selected, bounded_run and all result parsers are identical.
AST parsing, two-code-file scope and whitespace passed before execution.
Prior source-test failures and 24/28/37 checkpoints remain in their original reports.

Final own-record JSON/YAML, UTF-8, whitespace and 16 local Markdown references
passed direct checks. Raw stream hashes and all tested file hashes matched.
The complete message-file preflight passed for workflow
`2026-09-23-source-gates-implementation`; no history/aggregate mode was invoked.

## Remaining owners and reported target state

The coordinator reports target pilot review/admission passed and lease released.
That update is recorded as coordinator-reported target evidence; this task did not
re-run or independently read back the target. #368/#373 execute their own selected
checks. Historical failures are retained rather than treated as current failures
without reconciliation or replaced with this synthetic source-test pass.

The #382 driver exists in the integrated tree, but #369's native wrapper still
returns blocked. New native roots/caller/termination/provenance/hosted work requires
its separate authority; none is included here. The existing direct-child timeout
limitation is unchanged, and no new termination guarantee is claimed.

All seven old workflows and both successors, their context names and proposed
dispositions remain unchanged. No provider read/write occurred in this follow-up.
Policies stay dormant. Independent source review, integration/first push, exact
policy adoption and CI restoration remain coordinator/owner decisions. Unselected
legacy matrices and formal packets remain deferred-by-owner under U001.
