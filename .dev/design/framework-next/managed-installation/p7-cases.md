# P7 selection obligations (unexecuted)

All rows are **deferred-by-owner**, U001, owner program #322 coordinator / P7.
These are source-derived cases, not tests, fixtures, execution evidence or an
acceptance packet. P7 chooses focused new checks before pilot/root activation;
there is no instruction to restore the legacy full matrix.

| Area | Focused observations to select |
| --- | --- |
| Fixed engine | Isolated -I/-B entry, actual source/HEAD pin, every one of ten raw files, no ambient src/stdlib shadow, wrong module origin, missing/wrong PyYAML, missing same-engine checkout, no pycache. Standard library/host dependencies remain outside source authenticity claims. |
| Actual candidates | Real emitted metadata 1/2/3, null-config instruction packages, exact metadata/member closure, unsupported/duplicate/bool/float/version/path inputs and all reader budgets. No installer-created second metadata parser. |
| Preview/admission | Matching plan before/under lock; changed candidate/old lock/all-old member/protected input; pending mechanical gates; native domain/filesystem refusal; guard creation cannot change accepted deterministic plan hash. |
| No-op | Rebuilt same content with different time/run ID/current engine pin; unchanged bytes, modes and mtimes; no ID/run/snapshot/marker/lock allocation; absent/busy/unknown guard blocks without writes. Already-matching recovery also cannot create a guard. |
| Ownership | Same-byte unowned collision; unchanged old member drift; old/new case/prefix collision; unknown siblings and whole unknown subtrees preserved; no recursive traversal/removal of data/unknown stores. |
| Coordination | Two native processes, nonblocking exclusion during preparation/mutation/read-back; empty guard lock range and release on termination; guard replacement/links/hardlinks; no unlink/replace or guessed timeout to break a held lock. Caller declaration is not agent control. |
| Native publication | Windows NTFS/ReFS and Linux selected filesystems actually selected by P7; same-parent non-replacing add and replacing change, exact modes, denied/open-file publication, file/directory flush, delete, nested mount rejection. No power-loss or cross-file atomicity inference. |
| Capture | Before/after locks, every old/new managed member, all three metadata documents, dedup hashes, full reread before marker; missing/corrupt objects; interrupted capture and retained operation directories; only changed bytes staged. |
| Interruption | After guard/parent creation, partial object/operation capture, marker sibling creation/publication, each member boundary, mode-only change, lock publication, marker removal/read-back/unlock. Preserve failures/partial counts/residue; later matching does not erase an earlier failure. |
| Recovery | Exact operation/hash, same pin, expected current lock/marker, fresh exact scope; finish and restore from legitimate mixed before/after/defined-absence states; interrupted restore; complete exact sibling cleanup; unknown/partial siblings block. |
| Completed operation | Marker-free next state gives read-only already-matching; previous state requires fresh plan. Prepared upgrade without marker also cannot replay over a valid installation. Unknown/torn/newer locks/markers and active M01 remain untouched. |
| Whole loss | Recreated original root, all managed union/lock/markers absent, distinct surviving recovery volume, complete same-engine objects; missing protected inputs disclosed, conflicting present protected bytes block, project readiness remains not-assessed. Partial loss is refused. |
| Conservative reconciliation | First-install pre-record residue; empty guard after clean-install restore; partial/unknown .fi files; interruption during whole-loss reconstruction with ambiguous missing original members. Owner must choose any expanded recovery guarantee rather than introducing a hidden phase record or overwrite permission. |
| Budgets/outputs | Actual operation/sibling/object/control paths at 240/255 UTF-16 boundaries, fixed-width collision checks, aggregate bootstrap+writer read/entry budget, path-free failures, observed counts and partial changed including first guard/parent writes; interrupted output never emits a fabricated unchanged result. |

P7 must record actual backend/failure-domain observations and any remaining
refusals before a separately authorized pilot. Project activation/public-reader
checks, project source/config/data restoration, M01 need, CI restoration and
provider integration remain separate owners and decisions. No current result
supports an SSD, RAM-disk speed or durability improvement claim.
