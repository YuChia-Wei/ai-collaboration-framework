ILLUSTRATIVE FUTURE LAYOUT. No product files below were created by #326.
P1-A owns metadata/schema/operations. Tool lesson.fs is planned; entrypoint=null.
Only the design examples and issue workflow are delivered here.
Coordinator adopted tracked stable core/lock/exact runtime outputs as target layout.
P2 delivers a development candidate only; no released stable artifact is implied.

repository/
  src/                                      # manually editable PRODUCT source
    skills/lesson/
      SKILL.md
      skill-package.yaml                    # package-relative P1-A references
      schemas/lesson-record.schema.json     # lesson.record, schema_version 1.0.0
      templates/lesson.md
      references/configuration.md
      references/operations.md
                                              # no fake script for planned lesson.fs
    shared/                                 # no mandatory shared package for Lesson
    profiles/lesson-minimal.yaml            # selection; no payload copies
    adapters/codex/skill-entry.md.template
    distribution/manifest.yaml              # exact input/output mapping
  tools/build-development.py                # future source orchestration only
  tests/                                    # redesign deferred to P7
  docs/                                     # documentation, never implicit payload
  .ai/
    core/skills/lesson/                      # generated installed selection
      SKILL.md
      skill-package.yaml
      schemas/lesson-record.schema.json
      templates/lesson.md
      references/configuration.md
      references/operations.md
    custom/                                 # project-owned, excluded from packages
      framework.json                        # selected project JSON configuration
      rules/
      skills/team-specialization/
    framework.lock                          # actual generated installation identity
    local/                                  # ignored settings/cache
  .agents/skills/
    framework-lesson/SKILL.md                # exact adapter-owned file
    team-specialization/SKILL.md             # project-owned, preserved
  .dev/                                     # project rules/data/history, excluded
    lessons/                                # retained current records
    workflows/
    assessments/
    releases/
    standards/
  docs/lessons/                             # possible alternative configured store
  AGENTS.md                                 # project-owned installed entry reference
  AGENTS.zh-TW.md
  CLAUDE.md                                 # no new Claude adapter in first slice
  dist/                                     # disposable staging, future ignored

Future envelope for this exact selection:
  metadata/selection.json                   # generated identities/input digests
  metadata/files.json                       # generated path/owner/mode/digest list
  payload/.ai/core/skills/lesson/...          # ONLY the six manifest-listed members
  runtime/.agents/skills/framework-lesson/SKILL.md
                                              # generated, separately inventoried

No source .dev/, custom/, root AGENTS, local files or history are in the envelope.
The actual project's framework.lock is generated at installation; it is never
copied from the source project's dogfood lock.

Recovery uses a separately selected DURABLE store. Its small operation record
pairs prior core/lock/runtime with before-versions/backups of affected config,
records and indexes. Unchanged data roots are pinned, not copied wholesale.
It is not dist/ or RAM scratch. No recovery store was created by this issue.
