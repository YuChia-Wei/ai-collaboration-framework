# P6 selected maintenance contract

Coordinator selection under #322/U001 from #345 revised design `8a479ddc3204d0f031d9b9844aeb933af26f36f7`. Initial `51229b63565ce6e836d57a4b107cf6df5554bf7c` is retained as history. The revised [contract](installation-update/contract.md), [formats](installation-update/formats.md) and [cutover](installation-update/cutover-and-slices.md) are the selected design inputs; this document resolves scope and staging. No installation, migration or root activation has occurred.

| Decision | Selection |
| --- | --- |
| D345-01 | Adopt development-only candidate 1, engine/API/lock/operation 1 and quiescent maintenance. Caller stops affected sessions/tools/external writers. Mechanical result is managed-bytes-consistent; project readiness remains not-assessed. No ordinary invocation API. |
| D345-02 | Adopt exact owned byte/mode delta, all-old drift read, strict unowned collision, unknown preservation, immutable operation/marker and full durable managed before/after closure. This recovery guarantee costs snapshot I/O; no SSD or speed improvement is measured. |
| D345-03 | Select an external fixed source checkout and complete EnginePin, with maintenance-writer-only OS lock. No common skill launcher or adapter change. #354 implements read-only state/plan first; subsequent apply/recover owner closes the complete executing dependency/entry/bootstrap set and backend before any use. |
| D345-04 | Select lesson-minimal / lesson 0.2.0 / Codex as the first proposed P7/root pilot. It remains explicitly development output unless a separately authorized actual release exists. Broader profiles are selectable source only until later adoption. |
| D345-05 | M01 remains conditional: only a demonstrated existing closed P2 JSON config 1 needs 2. No current target need has been shown, so no converter implementation is assigned now. Project separately selects public-reader activation checks; no generic compatibility registry or data scan. |
| D345-06 | RAM is acceptable for explicit disposable scratch/staging, never inferred durable recovery. Actual pilot paths, failure domain, engine checkout/pin and tracking/attributes are selected with concrete P7/cutover execution. Windows 240 UTF-16 full-path / 255-segment conservative refusal budgets may guide source implementation; backend evidence is deferred, not certified. |

Lock/marker do not control agents already holding instructions. Hash rechecks detect observations, not elimination of TOCTOU. Nonparticipating concurrent writers are unsupported. A caller declaration is not technical attestation. Full RAM loss permits recovery only of wholly missing managed content at the selected original binding; source/config/data require separate owner recovery. Missing data never becomes whole-project-ready.

## Read-only implementation first

[Issue #354](https://github.com/YuChia-Wei/ai-collaboration-framework/issues/354) owns `src/distribution/installation_state.py`, `installation_plan.py`, any necessary same-owner `schemas/installation-*.schema.json`, its installation-planning design and its own workflow. It delivers exact candidate/lock readers, inspect/plan interfaces and deterministic delta/path/protected-input planning. It may define clear bounded limits and internal helpers within those files. It does not own shared loader/adapter/assembly/manifest/profile/root changes, mutation backend, CLI or product execution.

All actual metadata versions 1/2/3 are available through the delivered #346 loader. Reuse that owner instead of inventing a second parser. Candidate input is self-contained only for the files actually emitted by assembly: payload, generated runtime and three metadata documents. Source commit/tree, build-input identities, generator identities and profile provenance are not complete external source bytes or authenticity proof. Validate exact available hashes, cross-document identities, source/member/adapter relationships and package closure; identify unverifiable external provenance honestly. No fetch, full source reproduction or trust claim is implied. Unknown/unverifiable required mechanical binding returns an explicit unsupported/blocked result rather than fabricated proof.

The #354 API is a read-only subset of the selected design. It must not create a guard, marker, snapshot, config, store, install lock or even temporary directories. Engine identity validation is read-only. Its source handoff distinguishes the current reader/planner dependency set from the final mutation engine closure, which the later sole writer integrates after #354. Existing Python/PyYAML dependencies are not automatically installed. There is no source-completed claim for apply/recover/native durability.

After #354, assign one coherent maintenance writer unit for OS coordination, bootstrap, exact apply/recover and necessary direct state/plan integration. Splitting a tiny lock helper into a separate concurrent Issue is unnecessary. M01 follows only actual need. Root cutover follows P7-selected real checks and project activation decisions, preserving one active owner per capability and old history. #43/#305/#149/#168 retain their independent scope and acceptance.

Product CLI/help, schema validation, tests/fixtures, build/install/migration/compatibility, audit/lease machinery and CI remain deferred-by-owner under U001 to program #322 coordinator / P7. This design selects future behavior; direct source/JSON/AST inspection does not prove it.
