# P5 remaining capability selections

Coordinator selections under #322/U001 from #351 design `214c1c1c9bc6da26c1202e7a92d7d678d54b1e3b` and #352 design `ecf27befc599aa3ee589dff36d0597f0d1c24a8b`. Both are bounded designs, not product execution, installation or acceptance. Existing root routes and historical bytes stay under their current owners until explicit cutover.

## Frame and compliance: D351-01 through D351-05

Adopt the [CBF snapshot design](problem-frame-compliance/README.md), exact `problem-frame.cbf@1.0.0`, create-only new-identity snapshots, one owner for schema/producer/reader/structural validator/version handling and no migration edge. This is a new format; old YAML is not retroactively called v1. SWF/legacy machine support remains unsupported and preserved, with useful explicitly selected semantic reading and loss/uncertainty reporting. A later SWF implementation requires representative need and its own family contract.

Adopt a separate instruction-only compliance package, with explicit opt-in `dotnet@0.1.0` instructions. .NET language/SDK/library choices do not activate rules automatically. Structure, meaning and runtime outcomes stay distinct. Contradictions precede missing-evidence conclusions; empty scope or missing required evidence cannot pass. Complete criterion/assertion inventories bind to actual subject and evidence, without a universal 100% claim.

[Issue #356](https://github.com/YuChia-Wei/ai-collaboration-framework/issues/356) owns the ten-member `src/skills/problem-frame-author/` and six-member `src/skills/spec-compliance-validator/` together, plus its own problem-frame-implementation design/workflow. The same coherent source assignment can stabilize the producer's public structure result and its consumer without a duplicated parser or cross-task race. Skill identities and selectable dependencies remain separate and empty; instruction compliance may consume an explicit external owner reader where selected.

The [handoff](problem-frame-compliance/implementation-handoff.md) and [operation contract](problem-frame-compliance/operations-and-compliance.md) supply exact members/roles/configuration. Two explicit clarifications apply: every tool request has a required `operation` discriminator in its closed envelope; every unresolved statement OR assertion is linked from an open question. This resolves prose ambiguity without inventing evidence. The implementation must make its actual public structure-result fields precise and consume the same result in inspect/create/validate/render and compliance guidance.

Owned format/tool limits, source IDs, typed references, no-clobber creation, project configuration/write roots, caller templates and truthful published/uncertain failure reporting are selected together. No product tool, schema validator, primitive publication probe or fixture is executed during source authoring. Schema without a real reader/writer is incomplete; source completion still defers behavior to P7.

## Optional maintenance: D352-01 through D352-06

Adopt all six [optional-maintenance recommendations](optional-context-maintenance/README.md): auditor and governance independently selectable; prose/default caller export; project-format reuse through its existing owner; bounded project-owned edits; necessary rule/decision state with the existing owner; source/legacy recovery duties retained; optional P3 candidates when useful. No new assessment family, allocator/query/finalizer, customization ledger, universal rule engine or common runtime.

[Issue #357](https://github.com/YuChia-Wei/ai-collaboration-framework/issues/357) owns exactly three members each for `src/skills/ai-context-auditor/` (SKILL.md, skill-package.yaml, references/audit.md) and `src/skills/ai-context-governance/` (corresponding maintenance.md), plus its own optional-context-implementation design/workflow. Metadata 3/package 0.1.0, null config and empty schema/tool/role/dependency arrays; audit/compare and propose/apply are instruction operations. Split draft entry/method without source-design dependencies.

Governance direct edits follow actual task authorization and project ownership; they do not require another permission ritual when already authorized. No implicit core/projection/lock, provider, release, migration or root cutover. Optional knowledge handoff preserves candidate/accepted knowledge/proposal/adoption/rule bytes/effect distinctions and actual public-operation results. Current source policy, #316, finalized records, ledgers and active old recovery do not retire by selecting these new packages.

## Integration and verification

Neither source worker owns manifest/profile/loader/adapter/root/index changes. After real members return, the coordinator assigns one exact mapping owner. A separate context-maintenance convenience profile may select the two maintenance skills, but ordinary profiles must not acquire them implicitly. Frame/compliance profile selection also follows actual delivery. No speculative package rows or root activation.

#346 mapping and #347 engineering writes are separately blocked by automatic approval review awaiting direct owner confirmation; these new scopes do not replace those workers or evade those blocked actions. #354 read-only installation source is independent. P7 owns all actual new-tool/agent behavior, schema/compatibility/install/migration/backend tests and the later CI restoration decision. U001 direct content/syntax/reference/Git and complete commit-message checks are the current allowed evidence, not product passes.
