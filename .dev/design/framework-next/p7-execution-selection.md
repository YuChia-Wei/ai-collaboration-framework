# Selected P7 implementation sequence

Coordinator selection under program #322 / U001 after inspecting fixed designs
#364 `577f3922587ccd8cc8e314f5c558ad6991530886` and
#365 `c75582b08f67da17f9548f2eb38fe67c2e6e765e`.
The designs are retained proposals; this document resolves their shared execution
boundaries. Selection is not a test result, root activation or CI restoration.

## Selected contracts and first assignments

1. Adopt #364's [ownership and layers](verification-design/README.md), [bounded cases](verification-design/selected-checks.md)
   and seven real public families. C1-C3/C5 belong to V1 source contracts;
   package configuration C4 and applicable C6 boundaries belong to V2 public-family
   cases. Keep instruction observations separate from structural tests.
2. First V1 assignment owns `tests/framework_next/run.py`, `support.py`,
   `test_contracts.py` and a concise local README. Implement and run only selected
   contracts, including actual loaders and the bounded Lesson assembly/reader case.
   A real defect may be repaired in its existing distribution loader/selection/
   assembly/adapter owner if it preserves the accepted contract. Installation
   writer/recovery, skill payloads, shared manifest/profile and source policy are
   outside this repair scope; return concrete defects there to the coordinator.
3. In parallel, P1 owns dormant `.github` selector/workflow source, its focused
   selector/event tests, and the coherent prospective source-policy changes in
   [#365](pipeline-redesign/source-rules.md). It may exercise its own finite
   selector/outcome tests with labelled synthetic subprocess/provider inputs.
   It cannot invoke V1/V2/V3 commands before their implementations are integrated,
   execute a native trial, activate root skill routes or mutate provider settings.
4. After V1 publishes the small helper interface, independently assign V2 public
   families and V3 Windows installation. They own disjoint test files and existing
   source owners selected by their Issues. Do not create a second fixture framework
   or change shared helper behavior concurrently. V4 Lesson pilot follows selected
   contract/public/native evidence and an explicit residual disposition.

The first two Issues are created after this plan's checkpoint. Each receives one
independent `gpt-6-astra` / `ultra` conversation, exact starting commit, assigned
F: worktree, bounded writes and local commit handoff. No sub-agents, nested agents,
executor-created tasks, executor push/PR or callback messaging. Coordinator owns
shared indexes, integration, later assignment and online PR/merge.

## Runner and pipeline reconciliation

Select the small #364 command family:

```text
python -I -B tests/framework_next/run.py --layer contracts
python -I -B tests/framework_next/run.py --layer public --family lesson
python -I -B tests/framework_next/run.py --layer public
python -I -B tests/framework_next/run.py --layer native-windows --native-root EXPLICIT_ROOT
```

V1 publishes the exact implemented helper/argument/error interface. Later family
names are the seven package IDs from metadata; unknown or not-yet-implemented
selected layers/families fail explicitly, never return a green placeholder.
P1 binds to that actual interface before integration; if it differs from this
proposal, reconcile the small caller contract first. No automatic all-public run
for prose. Exact owner/member/reference comparisons cover all current selections;
the first physical candidate smoke remains Lesson. Broader actual profile assembly
and activation are selected when that profile is adopted; a Lesson result does not
prove all 18 packages were built or installed. Distribution-change admission must
state which logical and actual selection evidence it has and which affected
selection still requires an explicit trial; do not silently report blanket coverage.

Adopt one unconditional future PR context, `Source change gate`, and a separate
manual `Source native trial`. The native implementation starts with Windows and
the actually delivered V3 cases only. Do not advertise an Ubuntu execution path
or #365's illustrative case names before they have real command bindings and
accepted evidence. Additional platform support remains explicit follow-up scope.
Use immutable action revision pins in new workflow files where the verified action
release supports the selected runtime; this does not change the repository's
SHA-pinning enforcement setting. Keep token permissions minimal and credentials
out of checkout. Check definitions and failure selection, not simulated CI success.

Implement source-rule replacement as a small canonical prospective rule set and
precise applicability/pointer edits. Do not duplicate a new receipt, registry,
lease or history/reuse platform across every old policy. The affected policy,
YAML, root bilingual text and PR template must agree on dormant versus effective
scope. Existing published support/recovery remains explicitly owned. Actual
source-policy activation and U001 narrowing are recorded only at their selected
adoption boundary; merging dormant files does not activate them. High-risk policy/
installation/recovery changes receive a later separately assigned scoped review;
ordinary syntax checks are not independent review.

## Execution and storage bounds

This selection permits product imports, focused tests and tiny fixtures **only**
inside an implementation Issue's expressly assigned selected cases. It does not
reactivate legacy `check-all`, full/history/upgrade matrices, audit/lease packets,
benchmarks, package publication or hosted execution. The source redesign override
continues to govern every unselected obligation as `deferred-by-owner`.

Use `--output-root` / `FRAMEWORK_TEST_OUTPUT_ROOT` for new disposable fixtures;
zero-config behavior remains OS temporary allocation, with no drive discovery or
TEMP/TMP changes. For this program select isolated child roots below
`F:/framework-next/p7-runs/`, preserving `F:/ai-context-tests` and other worktrees.
The setting does not inherit or reinterpret legacy `AI_CONTEXT_TEST_TMP_ROOT`.

For later V3 choose an **explicit separate native root** below
`F:/framework-next/p7-runs/native-w01/`, subject to real native preflight; this
supersedes #364's proposed C: native fixture root. Native means actual Windows
filesystem/process behavior, not physical-media durability. Select only
`process-termination`, with OS/storage available. A refused backend is a reported
limitation, not permission to reroute silently. Persist only necessary recovery,
project backups and selected observations under distinct ignored C: parents in
`.dev/ai-context/local/p7/<run>/` as proposed by #364. No sole recovery copy on RAM.
Actual roots and cleanup ownership are bound in the later Issue before creation.

Retain #364's initial file/byte/process caps and stop-and-report outcomes; cheap
counts are observations, not a new acceptance framework. N5 gets one bounded
unmodified public interruption attempt. If missed, retain `not-observed`; N3 or
synthetic faults cannot fill it. Coordinator decides that actual residual before
root adoption, using observed public failure/recovery and exposure, not retry-until-green.
M01 remains unassigned without a real config-1 input. No downstream project,
provider write or old release support retirement is selected here.

## Adoption and restoration

After selected trials, coordinator assigns the already planned source-repository
Lesson/root adoption in a concrete isolated/quiescent window under U001. Routine
binding choices follow existing program authority; a genuinely new ownership,
credential, protection or support decision returns to the user. A fresh route
observation must be an explicitly coordinator-created independent conversation,
not an executor-created agent or a claim based only on entry text.

CI remains disabled. Present the final implemented workflow/context/settings set
and actual trial/review limitations for the user's explicit adoption. #365's
Actions settings proposal is not yet adopted; branch protections, rulesets,
credentials, release environment and seven disabled legacy workflows remain
unchanged. Enablement, hosted success and override retirement remain distinct.
